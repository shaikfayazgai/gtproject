---
phase: 05-enterprise-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/enterprise.ts
  - packages/types/src/index.ts
  - packages/types/src/sow.ts
  - packages/ui/src/components/otp-input/otp-input.tsx
  - packages/ui/src/components/otp-input/index.ts
  - packages/ui/src/index.ts
  - apps/enterprise-portal/package.json
  - apps/enterprise-portal/src/app/layout.tsx
  - apps/enterprise-portal/src/app/page.tsx
  - apps/enterprise-portal/src/app/(pre-auth)/layout.tsx
  - apps/enterprise-portal/src/app/(pre-auth)/page.tsx
  - apps/enterprise-portal/src/app/(pre-auth)/onboarding/layout.tsx
  - apps/enterprise-portal/src/app/(pre-auth)/onboarding/company/page.tsx
  - apps/enterprise-portal/src/app/(pre-auth)/onboarding/billing/page.tsx
  - apps/enterprise-portal/src/app/(pre-auth)/onboarding/team/page.tsx
  - apps/enterprise-portal/src/app/(pre-auth)/onboarding/first-sow/page.tsx
  - apps/enterprise-portal/src/components/onboarding/company-verification-step.tsx
  - apps/enterprise-portal/src/components/onboarding/billing-legal-step.tsx
  - apps/enterprise-portal/src/components/onboarding/team-setup-step.tsx
  - apps/enterprise-portal/src/components/onboarding/first-sow-step.tsx
  - apps/enterprise-portal/src/components/onboarding/index.ts
  - apps/enterprise-portal/src/app/(app)/layout.tsx
  - apps/enterprise-portal/src/app/(app)/sow/upload/page.tsx
  - apps/enterprise-portal/src/app/(app)/sow/[sowId]/intelligence/page.tsx
  - apps/enterprise-portal/src/app/(app)/sow/[sowId]/versions/page.tsx
  - apps/enterprise-portal/src/app/(app)/sow/archive/page.tsx
  - apps/enterprise-portal/src/components/sow/sow-upload-form.tsx
  - apps/enterprise-portal/src/components/sow/intelligence-display.tsx
  - apps/enterprise-portal/src/components/sow/version-history-list.tsx
  - apps/enterprise-portal/src/components/sow/sow-archive-table.tsx
  - apps/enterprise-portal/src/components/sow/index.ts
  - apps/enterprise-portal/src/store/auth-store.ts
  - apps/enterprise-portal/src/lib/msw/factories/common.ts
  - apps/enterprise-portal/src/lib/msw/factories/sow.ts
  - apps/enterprise-portal/src/lib/msw/handlers/auth.ts
  - apps/enterprise-portal/src/lib/msw/handlers/onboarding.ts
  - apps/enterprise-portal/src/lib/msw/handlers/sow.ts
  - apps/enterprise-portal/src/lib/msw/handlers/index.ts
  - apps/enterprise-portal/src/lib/msw/handlers.ts
  - apps/enterprise-portal/src/lib/msw/browser.ts
  - pnpm-lock.yaml
autonomous: true

must_haves:
  truths:
    - "Enterprise-specific types exist in @glimmora/types: EnterpriseOnboardingStepId, OrganizationProfile, TeamMember, PaymentPreferences, PaymentRecord, BlueprintTask, BlueprintMilestone, BlueprintPhase, Blueprint, SOWClause, SOWIntelligence, ESGReportData"
    - "OTPInput component is exported from @glimmora/ui wrapping Radix unstable_OneTimePasswordField with numeric validation and onComplete callback"
    - "Enterprise user can log in and complete 4-step onboarding: Company Verification, Billing & Legal, Team Setup, Upload First SOW"
    - "SOW upload page uses FileUpload component with accept='.pdf,.docx' and shows processing animation before redirecting to intelligence page"
    - "APG intelligence display shows extracted project objective, clauses, deliverables, timeline estimates, budget range, compliance flags, confidence score, and ambiguities"
    - "SOW version history shows a list of prior versions with versionNumber, dates, and download links"
    - "SOW archive shows all organization SOWs in a DataTable with version number, status, upload date, and an 'Upload New Version' action that passes existingSOWId to the upload form"
    - "SOW upload form checks for existingSOWId query param and if present, shows 'Uploading as new version' banner and passes existingSOWId in the upload FormData"
    - "lucide-react is installed as direct dependency in enterprise-portal"
  artifacts:
    - path: "packages/types/src/enterprise.ts"
      provides: "All enterprise-specific domain types (EP-01 through EP-26)"
      contains: "Blueprint"
    - path: "packages/ui/src/components/otp-input/otp-input.tsx"
      provides: "OTPInput component wrapping Radix unstable_OneTimePasswordField"
      contains: "OTPInput"
    - path: "apps/enterprise-portal/src/app/(app)/sow/upload/page.tsx"
      provides: "SOW upload page (EP-01)"
    - path: "apps/enterprise-portal/src/app/(app)/sow/[sowId]/intelligence/page.tsx"
      provides: "APG intelligence display page (EP-01)"
    - path: "apps/enterprise-portal/src/app/(app)/sow/archive/page.tsx"
      provides: "SOW archive page (EP-05)"
    - path: "apps/enterprise-portal/src/store/auth-store.ts"
      provides: "Enterprise auth store with Zustand persist"
  key_links:
    - from: "packages/ui/src/components/otp-input/otp-input.tsx"
      to: "radix-ui"
      via: "Wraps unstable_OneTimePasswordField as OTPInput"
      pattern: "import.*unstable_OneTimePasswordField.*from.*radix-ui"
    - from: "apps/enterprise-portal/src/components/sow/sow-upload-form.tsx"
      to: "@glimmora/ui FileUpload"
      via: "FileUpload with accept='.pdf,.docx' + processing animation"
      pattern: "import.*FileUpload.*from.*@glimmora/ui"
    - from: "apps/enterprise-portal/src/lib/msw/handlers/sow.ts"
      to: "packages/types/src/enterprise.ts"
      via: "SOWIntelligence type shapes mock APG response"
      pattern: "SOWIntelligence"
---

<objective>
Set up the Enterprise Portal foundation: enterprise-specific types in @glimmora/types, OTPInput component in @glimmora/ui, pre-auth login flow, 4-step enterprise onboarding, AppShell layout, SOW upload with mock APG processing, APG intelligence display, SOW version history, and SOW archive -- with MSW handlers for all endpoints.

Purpose: This is the foundation plan for Phase 5. It creates the type contracts all subsequent plans depend on, adds the OTPInput to the shared component library (needed by 05-02 for blueprint approval and 05-05 for payment release), and delivers the complete SOW management flow from upload through intelligence output, versions, and archive. Enterprise users land on a functional portal with login, onboarding, and the full SOW pipeline.

Output: @glimmora/types with enterprise.ts exports, @glimmora/ui with OTPInput, Enterprise Portal with pre-auth login, 4-step onboarding, AppShell layout with enterprise sidebar, SOW upload + APG intelligence + version history + archive pages, auth store, and MSW handlers for auth/onboarding/sow endpoints.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enterprise-portal/05-RESEARCH.md

Key codebase references -- patterns to follow:
@apps/women-portal/src/app/(pre-auth)/layout.tsx
@apps/women-portal/src/app/(pre-auth)/register/page.tsx
@apps/university-portal/src/store/auth-store.ts
@apps/university-portal/src/lib/msw/handlers/auth.ts
@apps/university-portal/src/lib/msw/handlers/index.ts
@apps/university-portal/src/lib/msw/factories/common.ts
@apps/mentor-portal/src/app/(pre-auth)/onboarding/layout.tsx
@apps/mentor-portal/src/app/(app)/layout.tsx (AppShell pattern)

Existing types to extend:
@packages/types/src/user.ts (EnterpriseUser already exists here -- enterprise.ts adds SOW/Blueprint/Payment types)
@packages/types/src/sow.ts (SOW and SOWDecomposition already exist -- enterprise.ts adds SOWClause, SOWIntelligence, Blueprint types)
@packages/types/src/project.ts (Project and ProjectMilestone already exist)
@packages/types/src/evidence.ts (Evidence and EvidencePack already exist)
@packages/types/src/index.ts

Component library:
@packages/ui/src/index.ts
@packages/ui/src/components/stepper/stepper.tsx
@packages/ui/src/components/file-upload/file-upload.tsx
@packages/ui/package.json

CRITICAL constraints:
- OTPInput wraps Radix `unstable_OneTimePasswordField` -- import as: `import { unstable_OneTimePasswordField as OneTimePasswordField } from 'radix-ui'`
- @glimmora/ui Label is a <p> element, NOT a <label> -- use native <label> HTML for htmlFor
- lucide-react must be added as direct dependency to enterprise-portal package.json
- MSW handlers go in handlers/ directory structure (not flat file)
- Enterprise sidebar nav: Dashboard, SOW Management (Upload, Archive), Projects (Active, Completed), Compliance (PoDL, ESG), Payments, Settings
- FileUpload component props: accept, maxFiles, maxSizeMB, onFilesChange
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enterprise types, OTPInput in @glimmora/ui, install lucide-react and @react-pdf/renderer + date-fns in enterprise-portal</name>
  <files>
    packages/types/src/enterprise.ts
    packages/types/src/index.ts
    packages/ui/src/components/otp-input/otp-input.tsx
    packages/ui/src/components/otp-input/index.ts
    packages/ui/src/index.ts
    apps/enterprise-portal/package.json
    pnpm-lock.yaml
  </files>
  <action>
**Step 1: Create `packages/types/src/enterprise.ts` with all enterprise domain types.**

This file contains types that don't already exist in sow.ts, project.ts, evidence.ts, or user.ts. The following types are NEW for the enterprise domain:

```typescript
// packages/types/src/enterprise.ts

export type EnterpriseOnboardingStepId = 'company' | 'billing' | 'team' | 'first-sow'

export interface EnterpriseOnboardingProgress {
  currentStep: EnterpriseOnboardingStepId
  completedSteps: EnterpriseOnboardingStepId[]
  companyVerified: boolean
}

export type TeamMemberRole = 'admin' | 'project-manager' | 'finance-approver' | 'viewer'

export interface TeamMember {
  id: string
  email: string
  name: string
  role: TeamMemberRole
  invitedAt: string
  acceptedAt?: string
  isActive: boolean
}

export interface OrganizationProfile {
  id: string
  name: string
  logoUrl?: string
  industry: string
  size: '1-50' | '51-200' | '201-1000' | '1000+'
  headquarters: string
  website?: string
  primaryContactEmail: string
  taxId: string
  billingCurrency: string
  billingContactEmail: string
  verificationStatus: 'pending' | 'verified' | 'rejected'
}

export type PaymentReleaseMode = 'manual' | 'auto-on-approval' | 'apg-silent'

export interface PaymentPreferences {
  defaultMode: PaymentReleaseMode
  apgSilentThresholdAmount?: number
  autoReleaseDelayDays?: number
}

export interface PaymentRecord {
  id: string
  projectId: string
  milestoneId: string
  evidencePackId: string
  amount: number
  platformFee: number
  netToContributor: number
  currency: string
  status: 'pending' | 'released' | 'held' | 'disputed'
  releaseMode: PaymentReleaseMode
  releasedAt?: string
  transactionId?: string
  holdReason?: string
  holdExpiresAt?: string
}

export interface BlueprintTask {
  id: string
  title: string
  description: string
  skillRequirements: string[]
  estimatedHours: number
  evidenceTypeExpected: string
  acceptanceCriteria: string
  dependsOn: string[]
  clauseIds: string[]  // Links to SOW clauses
}

export interface BlueprintMilestone {
  id: string
  name: string
  description: string
  successCriteria: string
  taskIds: string[]
  budgetAllocation: number
  targetWeek: number
  paymentTrigger: PaymentReleaseMode
}

export interface BlueprintPhase {
  id: string
  name: string
  description: string
  startDate: string
  endDate: string
  milestoneIds: string[]
}

export interface Blueprint {
  id: string
  sowId: string
  projectName: string
  phases: BlueprintPhase[]
  milestones: BlueprintMilestone[]
  tasks: BlueprintTask[]
  totalBudget: number
  timeline: { startDate: string; endDate: string }
  completionPercentage: number
  issues: string[]
  status: 'draft' | 'review' | 'approved'
}

export interface SOWClause {
  id: string
  text: string
  type: 'objective' | 'deliverable' | 'timeline' | 'budget' | 'compliance' | 'general'
  confidence: number
  linkedTaskIds: string[]
}

export interface SOWIntelligence {
  sowId: string
  projectObjective: string
  clauses: SOWClause[]
  deliverables: Array<{ text: string; included: boolean }>
  timelineEstimates: Array<{ milestone: string; date: string }>
  budgetRange: { min: number; max: number; currency: string }
  complianceFlags: Array<{ item: string; severity: 'mandatory' | 'preferred' | 'note' }>
  confidenceScore: number
  ambiguities: Array<{ section: string; issue: string }>
}

export interface ESGReportData {
  organizationName: string
  reportPeriod: string
  generatedDate: string
  womenContributorHours: number
  studentContributorHours: number
  totalContributorHours: number
  womenWorkforcePercentage: number
  studentWorkforcePercentage: number
  underrepresentedGroupPercentage: number
  podlCredentialsIssued: number
  skillsDeveloped: string[]
  totalPaymentsReleased: number
  onTimePaymentRate: number
  currency: string
  onTimeDeliveryRate: number
  reworkRate: number
  mentorReviewRate: number
}
```

**Step 1b: Add version tracking fields to `packages/types/src/sow.ts`.**

Add two optional fields to the existing `SOW` interface (non-breaking additions):
```typescript
export interface SOW {
  // ... existing fields ...
  versionNumber?: number        // 1 for first upload, 2+ for subsequent versions
  parentSowId?: string          // links to the previous version SOW id
}
```

This enables EP-04: "each iteration tracked" -- when an enterprise user uploads a revised SOW from the archive table, the new SOW links back to the parent via parentSowId.

**Step 2: Update `packages/types/src/index.ts` to re-export enterprise types.**

Add at the end:
```typescript
export type {
  EnterpriseOnboardingStepId, EnterpriseOnboardingProgress,
  TeamMemberRole, TeamMember, OrganizationProfile,
  PaymentReleaseMode, PaymentPreferences, PaymentRecord,
  BlueprintTask, BlueprintMilestone, BlueprintPhase, Blueprint,
  SOWClause, SOWIntelligence, ESGReportData,
} from './enterprise'
```

Note: EnterpriseUser already exists in user.ts and is already exported. SOW, SOWDecomposition are in sow.ts. Project, ProjectMilestone are in project.ts. Evidence, EvidencePack are in evidence.ts. All already exported.

**Step 3: Create OTPInput component in @glimmora/ui.**

Create directory `packages/ui/src/components/otp-input/`.

**`packages/ui/src/components/otp-input/otp-input.tsx`:**
```typescript
'use client'
import { unstable_OneTimePasswordField as OneTimePasswordField } from 'radix-ui'
import { cn } from '../../lib/utils'

interface OTPInputProps {
  length?: number
  onComplete?: (value: string) => void
  className?: string
}

export function OTPInput({ length = 6, onComplete, className }: OTPInputProps) {
  return (
    <OneTimePasswordField.Root
      validationType="numeric"
      onValueChange={(value: string) => {
        if (value.length === length) {
          onComplete?.(value)
        }
      }}
      className={cn('flex gap-2', className)}
    >
      {Array.from({ length }).map((_, i) => (
        <OneTimePasswordField.Input
          key={i}
          className={cn(
            'h-12 w-10 rounded-inner border border-border bg-bg-card text-center',
            'text-lg font-mono text-text-heading',
            'focus:border-brand-primary focus:ring-1 focus:ring-brand-primary',
            'outline-none transition-colors'
          )}
        />
      ))}
      <OneTimePasswordField.HiddenInput />
    </OneTimePasswordField.Root>
  )
}
```

**`packages/ui/src/components/otp-input/index.ts`:**
```typescript
export { OTPInput } from './otp-input'
```

**Step 4: Export OTPInput from @glimmora/ui index.**

Add to `packages/ui/src/index.ts`:
```typescript
// DS-48: OTP Input
export { OTPInput } from './components/otp-input'
```

**Step 5: Install lucide-react, @react-pdf/renderer, and date-fns in enterprise-portal.**

```bash
pnpm add lucide-react @react-pdf/renderer date-fns --filter @glimmora/enterprise-portal
```

lucide-react is needed for sidebar icons and various UI icons throughout the portal.
@react-pdf/renderer is needed for PoDL audit and ESG compliance PDF export (EP-20, EP-21).
date-fns is needed for Gantt chart date formatting (EP-09).
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/ui` -- builds without errors.
Run `grep "Blueprint" packages/types/src/enterprise.ts` -- must match.
Run `grep "enterprise" packages/types/src/index.ts` -- must match enterprise exports.
Run `grep "OTPInput" packages/ui/src/index.ts` -- must match.
Run `grep "unstable_OneTimePasswordField" packages/ui/src/components/otp-input/otp-input.tsx` -- must match.
Run `grep "lucide-react" apps/enterprise-portal/package.json` -- must match.
Run `grep "@react-pdf/renderer" apps/enterprise-portal/package.json` -- must match.
Run `grep "date-fns" apps/enterprise-portal/package.json` -- must match.
  </verify>
  <done>@glimmora/types has enterprise.ts with all domain types (EnterpriseOnboardingProgress, OrganizationProfile, TeamMember, PaymentPreferences, PaymentRecord, BlueprintTask, BlueprintMilestone, BlueprintPhase, Blueprint, SOWClause, SOWIntelligence, ESGReportData). @glimmora/ui has OTPInput wrapping Radix unstable_OneTimePasswordField. lucide-react, @react-pdf/renderer, and date-fns installed in enterprise-portal.</done>
</task>

<task type="auto">
  <name>Task 2: Build pre-auth login, 4-step onboarding, AppShell layout, and auth store</name>
  <files>
    apps/enterprise-portal/src/app/layout.tsx
    apps/enterprise-portal/src/app/page.tsx
    apps/enterprise-portal/src/app/(pre-auth)/layout.tsx
    apps/enterprise-portal/src/app/(pre-auth)/page.tsx
    apps/enterprise-portal/src/app/(pre-auth)/onboarding/layout.tsx
    apps/enterprise-portal/src/app/(pre-auth)/onboarding/company/page.tsx
    apps/enterprise-portal/src/app/(pre-auth)/onboarding/billing/page.tsx
    apps/enterprise-portal/src/app/(pre-auth)/onboarding/team/page.tsx
    apps/enterprise-portal/src/app/(pre-auth)/onboarding/first-sow/page.tsx
    apps/enterprise-portal/src/components/onboarding/company-verification-step.tsx
    apps/enterprise-portal/src/components/onboarding/billing-legal-step.tsx
    apps/enterprise-portal/src/components/onboarding/team-setup-step.tsx
    apps/enterprise-portal/src/components/onboarding/first-sow-step.tsx
    apps/enterprise-portal/src/components/onboarding/index.ts
    apps/enterprise-portal/src/app/(app)/layout.tsx
    apps/enterprise-portal/src/store/auth-store.ts
  </files>
  <action>
**Step 1: Remove canary page and set up route groups.**

Delete the canary content from `apps/enterprise-portal/src/app/page.tsx`. Replace it with a redirect to `/(pre-auth)`.

Update `apps/enterprise-portal/src/app/layout.tsx` to include Providers wrapper (same pattern as university-portal). The root layout wraps children in Providers (MSWProvider + QueryProvider). Keep `globals.css` import.

Update `apps/enterprise-portal/src/app/page.tsx` to redirect:
```typescript
import { redirect } from 'next/navigation'
export default function Home() { redirect('/login') }
```

Wait -- the pre-auth page IS the login. Use the (pre-auth) route group's page.tsx as the login page.

**Step 2: Create pre-auth layout and login page.**

**`apps/enterprise-portal/src/app/(pre-auth)/layout.tsx`:**
Same pattern as women-portal pre-auth layout -- centered container with max-w-md, bg-bg-app.

**`apps/enterprise-portal/src/app/(pre-auth)/page.tsx`:**
Enterprise login page with:
- GlimmoraTeam enterprise branding (Heading + Body)
- Login form (email + password) calling POST /api/enterprise/auth/login
- On successful login, check user status from auth store:
  - If `!onboardingCompleted` -> redirect to /onboarding/company
  - If `onboardingCompleted` -> redirect to /dashboard
- Use native `<label>` for form field labels (NOT @glimmora/ui Label which is a `<p>`)

**Step 3: Create 4-step onboarding.**

**`apps/enterprise-portal/src/app/(pre-auth)/onboarding/layout.tsx`:**
Onboarding layout with Stepper component (from @glimmora/ui) showing 4 steps:
1. "Company Verification"
2. "Billing & Legal"
3. "Team Setup"
4. "Upload First SOW"

Derive current step from pathname (same pattern as mentor-portal onboarding layout).

**`apps/enterprise-portal/src/components/onboarding/company-verification-step.tsx`:**
Fields: Company Name, Industry (Select), Company Size (Select: 1-50, 51-200, 201-1000, 1000+), Headquarters (text), Website (optional text), Tax ID (text). On submit POST /api/enterprise/onboarding/company, navigate to /onboarding/billing.

**`apps/enterprise-portal/src/components/onboarding/billing-legal-step.tsx`:**
Fields: Billing Contact Email, Billing Currency (Select: USD, EUR, GBP, INR, AED), Payment Terms acceptance checkbox ("I accept the platform payment terms and conditions"). On submit POST /api/enterprise/onboarding/billing, navigate to /onboarding/team.

**`apps/enterprise-portal/src/components/onboarding/team-setup-step.tsx`:**
Fields: Invite team members section -- a simple repeatable row with Email + Role (Select: project-manager, finance-approver, viewer) + "Add Another" button. Minimum 0 members (skip allowed). On submit POST /api/enterprise/onboarding/team, navigate to /onboarding/first-sow.

**`apps/enterprise-portal/src/components/onboarding/first-sow-step.tsx`:**
Uses FileUpload component with accept=".pdf,.docx" and maxFiles={1}, maxSizeMB={50}. Has a "Skip for now" link and an "Upload & Continue" button. On upload success, sets onboardingCompleted in auth store and redirects to /dashboard.

All onboarding pages import their step component and render in a consistent card container.

**`apps/enterprise-portal/src/components/onboarding/index.ts`:**
Barrel export all step components.

**Step 4: Create AppShell (app) layout.**

**`apps/enterprise-portal/src/app/(app)/layout.tsx`:**
Uses AppShell from @glimmora/ui with Sidebar. Enterprise sidebar nav items (using lucide-react icons):
- Dashboard (LayoutDashboard icon) -> /dashboard
- SOW Management section:
  - Upload SOW (Upload icon) -> /sow/upload
  - SOW Archive (Archive icon) -> /sow/archive
- Projects section:
  - Active Projects (FolderKanban icon) -> /projects
  - Completed (CheckCircle icon) -> /projects/completed
- Compliance section:
  - PoDL Reports (FileCheck icon) -> /compliance/podl
  - ESG Reports (Leaf icon) -> /compliance/esg
- Payments (CreditCard icon) -> /payments
- Settings (Settings icon) -> /settings

Follow the exact AppShell + Sidebar + TopBar pattern from mentor-portal (app) layout.

**Step 5: Create auth store.**

**`apps/enterprise-portal/src/store/auth-store.ts`:**
```typescript
import { create } from 'zustand'
import { persist, createJSONStorage } from 'zustand/middleware'
import type { EnterpriseUser } from '@glimmora/types'

interface AuthState {
  user: EnterpriseUser | null
  isAuthenticated: boolean
  onboardingStep: number
  onboardingCompleted: boolean
  setUser: (user: EnterpriseUser) => void
  setOnboardingStep: (step: number) => void
  completeOnboarding: () => void
  logout: () => void
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      isAuthenticated: false,
      onboardingStep: 0,
      onboardingCompleted: false,
      setUser: (user) => set({ user, isAuthenticated: true }),
      setOnboardingStep: (step) => set({ onboardingStep: step }),
      completeOnboarding: () => set({ onboardingCompleted: true, onboardingStep: 4 }),
      logout: () => set({ user: null, isAuthenticated: false, onboardingStep: 0, onboardingCompleted: false }),
    }),
    { name: 'glimmora-enterprise-auth', storage: createJSONStorage(() => localStorage) }
  )
)
```

  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/enterprise-portal` -- builds without errors.
Verify pre-auth pages exist: `ls apps/enterprise-portal/src/app/\(pre-auth\)/page.tsx` -- exists.
Verify onboarding pages: `ls apps/enterprise-portal/src/app/\(pre-auth\)/onboarding/{company,billing,team,first-sow}/page.tsx` -- all 4 exist.
Verify AppShell layout: `grep "AppShell" apps/enterprise-portal/src/app/\(app\)/layout.tsx` -- must match.
Verify auth store: `grep "onboardingCompleted" apps/enterprise-portal/src/store/auth-store.ts` -- must match.
  </verify>
  <done>Enterprise Portal pre-auth flow: root layout with Providers, login page, 4-step onboarding (company verification, billing & legal, team setup, first SOW upload) with Stepper, AppShell layout with enterprise sidebar navigation. Auth store with Zustand persist and onboarding state.</done>
</task>

<task type="auto">
  <name>Task 3: Build SOW upload, APG intelligence display, version history, SOW archive, and all MSW handlers</name>
  <files>
    apps/enterprise-portal/src/app/(app)/sow/upload/page.tsx
    apps/enterprise-portal/src/app/(app)/sow/[sowId]/intelligence/page.tsx
    apps/enterprise-portal/src/app/(app)/sow/[sowId]/versions/page.tsx
    apps/enterprise-portal/src/app/(app)/sow/archive/page.tsx
    apps/enterprise-portal/src/components/sow/sow-upload-form.tsx
    apps/enterprise-portal/src/components/sow/intelligence-display.tsx
    apps/enterprise-portal/src/components/sow/version-history-list.tsx
    apps/enterprise-portal/src/components/sow/sow-archive-table.tsx
    apps/enterprise-portal/src/components/sow/index.ts
    apps/enterprise-portal/src/lib/msw/factories/common.ts
    apps/enterprise-portal/src/lib/msw/factories/sow.ts
    apps/enterprise-portal/src/lib/msw/handlers/auth.ts
    apps/enterprise-portal/src/lib/msw/handlers/onboarding.ts
    apps/enterprise-portal/src/lib/msw/handlers/sow.ts
    apps/enterprise-portal/src/lib/msw/handlers/index.ts
    apps/enterprise-portal/src/lib/msw/handlers.ts
    apps/enterprise-portal/src/lib/msw/browser.ts
  </files>
  <action>
**Step 1: Create SOW upload page.**

**`apps/enterprise-portal/src/app/(app)/sow/upload/page.tsx`:**
Renders PageHeader with title "Upload Statement of Work" and the SOWUploadForm component.

**`apps/enterprise-portal/src/components/sow/sow-upload-form.tsx`:**
```
'use client'
```
Uses FileUpload from @glimmora/ui with accept=".pdf,.docx", maxFiles={1}, maxSizeMB={50}. Read `existingSOWId` from URL search params via `useSearchParams()`. If present, show a banner: "Uploading as new version of existing SOW". On file selected, shows "Upload & Analyze SOW" button (or "Upload New Version" if existingSOWId present). On click, calls POST /api/enterprise/sow/upload with FormData including `existingSOWId` if present. Shows processing states:
- 'uploading': Spinner + "Uploading document..."
- 'processing': Spinner + "APG is analyzing your SOW -- extracting tasks, skills, and timeline..."
- 'complete': redirect to `/sow/${sowId}/intelligence`
- 'error': error message with retry button

Follow the exact code pattern from 05-RESEARCH.md Pattern 4 (SOW Upload with Processing Animation).

**Step 2: Create APG intelligence display page.**

**`apps/enterprise-portal/src/app/(app)/sow/[sowId]/intelligence/page.tsx`:**
Fetches GET /api/enterprise/sow/:sowId/intelligence via TanStack useQuery. Shows loading spinner while fetching.

**`apps/enterprise-portal/src/components/sow/intelligence-display.tsx`:**
```
'use client'
```
Displays SOWIntelligence data in organized sections:
1. **Project Objective** -- heading + body text
2. **Confidence Score** -- Progress component showing percentage + color (green >0.8, amber >0.6, red otherwise)
3. **Extracted Clauses** -- list of SOWClause items grouped by type (objective, deliverable, timeline, budget, compliance, general) using Badge for type and confidence
4. **Deliverables** -- checklist showing included/excluded items
5. **Timeline Estimates** -- table of milestone + target date
6. **Budget Range** -- min-max range display with currency
7. **Compliance Flags** -- list with severity badges (mandatory=red, preferred=amber, note=grey)
8. **Ambiguities** -- warning section listing sections with issues (amber background)
9. **Actions:** "Open Blueprint Editor" button -> `/projects/new?sowId=${sowId}`, "View Version History" link -> `/sow/${sowId}/versions`

**Step 3: Create SOW version history page.**

**`apps/enterprise-portal/src/app/(app)/sow/[sowId]/versions/page.tsx`:**
Fetches GET /api/enterprise/sow/:sowId/versions via TanStack useQuery.

**`apps/enterprise-portal/src/components/sow/version-history-list.tsx`:**
```
'use client'
```
Simple list/timeline showing SOW versions:
- Version number (versionNumber as "v1", "v2"), upload date, file name, file size
- Status badge (uploaded, parsing, parsed, decomposed, blueprint-ready, approved)
- "Download" link for each version
- Most recent version highlighted at top

**Step 4: Create SOW archive page.**

**`apps/enterprise-portal/src/app/(app)/sow/archive/page.tsx`:**
Fetches GET /api/enterprise/sow/archive via TanStack useQuery.

**`apps/enterprise-portal/src/components/sow/sow-archive-table.tsx`:**
```
'use client'
```
DataTable showing all organization SOWs with columns:
- SOW Name (fileName)
- Version (versionNumber displayed as "v1", "v2")
- Status (Badge)
- Upload Date (formatted)
- Tasks Extracted (number)
- Actions (View Intelligence, Open Editor, **Upload New Version** (navigates to `/sow/upload?existingSOWId=${sow.id}`), Archive)

The "Upload New Version" action enables EP-04 version tracking -- enterprise users can upload a revised SOW that links to the existing SOW and increments the version counter.

**Step 5: Create MSW factories and handlers.**

**`apps/enterprise-portal/src/lib/msw/factories/common.ts`:**
Copy randomId and isoNow from women-portal/src/lib/msw/factories/common.ts.

**`apps/enterprise-portal/src/lib/msw/factories/sow.ts`:**
Factory functions:
- `createMockSOW()` -- returns SOW with status 'parsed', extracted tasks/skills, `versionNumber: 1`
- `createMockSOWIntelligence(sowId: string)` -- returns SOWIntelligence with realistic mock data: 5-6 clauses of different types, 4-5 deliverables, 3-4 timeline estimates, budget range, 2 compliance flags, 1 ambiguity, confidence 0.87
- `createMockSOWVersions(sowId: string)` -- returns array of 3 SOW objects with `versionNumber: 1/2/3`, different dates and statuses (v1: approved, v2: parsed, v3: blueprint-ready)
- `createMockSOWArchive()` -- returns array of 5 SOW objects with various statuses and `versionNumber: 1` (or higher for some)

**MSW handlers:**

**`handlers/auth.ts`:**
- POST /api/enterprise/auth/login -> returns mock token + EnterpriseUser with organizationId and organizationName

**`handlers/onboarding.ts`:**
- GET /api/enterprise/onboarding -> returns EnterpriseOnboardingProgress
- PATCH /api/enterprise/onboarding/:step -> 200

**`handlers/sow.ts`:**
- POST /api/enterprise/sow/upload -> delay(2000). Check body for `existingSOWId`. If present (new version upload), return `{ sowId: randomId(), versionNumber: 2, parentSowId: existingSOWId, status: 'parsed', extractedTasks: 14, ... }`. If absent (new SOW), return `{ sowId: 'sow-001', versionNumber: 1, status: 'parsed', extractedTasks: 12, extractedSkills: [...], estimatedTimeline: '12 weeks', estimatedBudget: 250000, confidenceScore: 0.87 }`
- GET /api/enterprise/sow/:sowId/intelligence -> returns createMockSOWIntelligence(sowId)
- GET /api/enterprise/sow/:sowId/versions -> returns createMockSOWVersions(sowId)
- GET /api/enterprise/sow/archive -> returns createMockSOWArchive()
- DELETE /api/enterprise/sow/:sowId -> 204

**`handlers/index.ts`:**
Aggregates and re-exports all handlers from auth, onboarding, sow.

**`handlers.ts`** (root msw dir): Re-exports from handlers/index.ts.

Update **`browser.ts`** if needed to import from the new handlers path.

**`apps/enterprise-portal/src/components/sow/index.ts`:**
Barrel export all SOW components.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/enterprise-portal` -- builds without errors.
Verify SOW pages: `ls apps/enterprise-portal/src/app/\(app\)/sow/upload/page.tsx apps/enterprise-portal/src/app/\(app\)/sow/archive/page.tsx` -- both exist.
Verify intelligence page: `ls apps/enterprise-portal/src/app/\(app\)/sow/\[sowId\]/intelligence/page.tsx` -- exists.
Verify versions page: `ls apps/enterprise-portal/src/app/\(app\)/sow/\[sowId\]/versions/page.tsx` -- exists.
Verify FileUpload + existingSOWId: `grep "existingSOWId\|FileUpload" apps/enterprise-portal/src/components/sow/sow-upload-form.tsx` -- must match both.
Verify intelligence display sections: `grep "SOWIntelligence\|confidenceScore\|ambiguities" apps/enterprise-portal/src/components/sow/intelligence-display.tsx` -- must match.
Verify version numbers: `grep "versionNumber" apps/enterprise-portal/src/components/sow/version-history-list.tsx` -- must match.
Verify archive Upload New Version: `grep "existingSOWId\|Upload New Version" apps/enterprise-portal/src/components/sow/sow-archive-table.tsx` -- must match (EP-04).
Verify MSW handler structure: `ls apps/enterprise-portal/src/lib/msw/handlers/{auth,onboarding,sow,index}.ts` -- all exist.
Verify MSW upload version handling: `grep "existingSOWId\|versionNumber" apps/enterprise-portal/src/lib/msw/handlers/sow.ts` -- must match.
  </verify>
  <done>SOW management pipeline: upload form with version-aware upload (existingSOWId support for EP-04), APG intelligence display with all SOWIntelligence fields, version history timeline with versionNumber, archive DataTable with "Upload New Version" action. MSW handlers for all auth/onboarding/SOW endpoints with realistic mock data.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/ui --filter=@glimmora/enterprise-portal` builds without errors
2. `packages/types/src/enterprise.ts` exports all enterprise domain types (Blueprint, SOWClause, SOWIntelligence, PaymentRecord, OrganizationProfile, TeamMember, ESGReportData)
3. `packages/ui/src/index.ts` exports OTPInput
4. OTPInput wraps Radix `unstable_OneTimePasswordField` with numeric validation
5. `lucide-react`, `@react-pdf/renderer`, `date-fns` in enterprise-portal package.json
6. Enterprise login page with email/password form
7. 4-step onboarding: Company, Billing, Team, First SOW -- with Stepper
8. AppShell layout with enterprise sidebar (Dashboard, SOW Management, Projects, Compliance, Payments, Settings)
9. SOW upload uses FileUpload with accept=".pdf,.docx" and shows uploading/processing/complete states
10. Intelligence display shows all SOWIntelligence fields: objective, clauses by type, deliverables checklist, timeline, budget range, compliance flags, ambiguities
11. Version history shows SOW versions with status badges
12. SOW archive DataTable with status, date, task count columns
13. MSW handlers in handlers/ directory: auth.ts, onboarding.ts, sow.ts, index.ts
</verification>

<success_criteria>
- Enterprise types are available in @glimmora/types for all subsequent plans to import
- OTPInput component is available in @glimmora/ui for Plans 05-02 (blueprint approval) and 05-05 (payment release)
- Enterprise user can log in, complete onboarding, and reach the dashboard
- SOW upload flow works: file upload -> processing animation -> APG intelligence display with all extracted data
- SOW version history and archive are navigable
- All pages build without errors and follow established patterns from prior portals
</success_criteria>

<output>
After completion, create `.planning/phases/05-enterprise-portal/05-01-SUMMARY.md`
</output>

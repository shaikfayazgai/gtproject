---
phase: 05-enterprise-portal
plan: 06
type: execute
wave: 5
depends_on: ["05-02", "05-04", "05-05"]
files_modified:
  - apps/enterprise-portal/src/app/(app)/compliance/page.tsx
  - apps/enterprise-portal/src/app/(app)/compliance/podl/page.tsx
  - apps/enterprise-portal/src/app/(app)/compliance/esg/page.tsx
  - apps/enterprise-portal/src/components/compliance/podl-report-pdf.tsx
  - apps/enterprise-portal/src/components/compliance/esg-report-pdf.tsx
  - apps/enterprise-portal/src/components/compliance/podl-export-form.tsx
  - apps/enterprise-portal/src/components/compliance/esg-export-form.tsx
  - apps/enterprise-portal/src/components/compliance/index.ts
  - apps/enterprise-portal/src/app/(app)/projects/completed/page.tsx
  - apps/enterprise-portal/src/app/(app)/payments/page.tsx
  - apps/enterprise-portal/src/app/(app)/payments/settings/page.tsx
  - apps/enterprise-portal/src/components/payments/payment-history-table.tsx
  - apps/enterprise-portal/src/components/payments/pending-approvals-table.tsx
  - apps/enterprise-portal/src/components/payments/payment-settings-form.tsx
  - apps/enterprise-portal/src/components/payments/index.ts
  - apps/enterprise-portal/src/app/(app)/settings/page.tsx
  - apps/enterprise-portal/src/app/(app)/settings/organization/page.tsx
  - apps/enterprise-portal/src/app/(app)/settings/team/page.tsx
  - apps/enterprise-portal/src/app/(app)/settings/notifications/page.tsx
  - apps/enterprise-portal/src/lib/msw/handlers/compliance.ts
  - apps/enterprise-portal/src/lib/msw/handlers/settings.ts
  - apps/enterprise-portal/src/lib/msw/handlers/index.ts
autonomous: true

must_haves:
  truths:
    - "PoDL audit report PDF uses @react-pdf/renderer via dynamic import (await import) to avoid SSR crashes"
    - "ESG compliance report PDF renders GRI-aligned sections: Workforce Diversity, Skills Development, Fair Payment, Delivery Quality"
    - "PDF export uses brand colors (#A0614A terracotta header, #6B4C3B subtitles) matching GlimmoraTeam brand identity"
    - "Completed projects archive shows finished projects with PoDL trail link"
    - "Organization profile management allows editing name, logo URL, industry, size, headquarters"
    - "Team access management shows DataTable of team members with role, status, and invite/remove actions"
    - "Payment release preferences page allows choosing default mode (manual/auto/apg-silent) with threshold"
    - "Notification preferences page shows channel/category grid with toggles"
  artifacts:
    - path: "apps/enterprise-portal/src/components/compliance/podl-report-pdf.tsx"
      provides: "PoDL audit report PDF document (EP-20)"
      contains: "PoDLReportPDF"
    - path: "apps/enterprise-portal/src/components/compliance/esg-report-pdf.tsx"
      provides: "ESG compliance report PDF document (EP-21)"
      contains: "ESGReportPDF"
    - path: "apps/enterprise-portal/src/app/(app)/projects/completed/page.tsx"
      provides: "Completed projects archive (EP-22)"
    - path: "apps/enterprise-portal/src/app/(app)/settings/organization/page.tsx"
      provides: "Organization profile management (EP-23)"
    - path: "apps/enterprise-portal/src/app/(app)/settings/team/page.tsx"
      provides: "Team access management (EP-24)"
    - path: "apps/enterprise-portal/src/app/(app)/payments/settings/page.tsx"
      provides: "Payment release preferences (EP-25)"
    - path: "apps/enterprise-portal/src/app/(app)/settings/notifications/page.tsx"
      provides: "Notification preferences (EP-26)"
  key_links:
    - from: "apps/enterprise-portal/src/components/compliance/podl-export-form.tsx"
      to: "@react-pdf/renderer"
      via: "Dynamic import for PDF generation"
      pattern: "await import.*@react-pdf/renderer"
    - from: "apps/enterprise-portal/src/components/compliance/esg-report-pdf.tsx"
      to: "@react-pdf/renderer"
      via: "Document/Page/Text/View/StyleSheet from @react-pdf/renderer"
      pattern: "import.*Document.*Page.*Text.*View.*StyleSheet.*from.*@react-pdf/renderer"
    - from: "apps/enterprise-portal/src/app/(app)/settings/team/page.tsx"
      to: "@glimmora/ui DataTable"
      via: "Team member management table"
      pattern: "import.*DataTable.*from.*@glimmora/ui"
---

<objective>
Build PoDL and ESG compliance report PDF exports, completed projects archive, enterprise settings (organization profile, team access, payment preferences, notification preferences), and payments hub -- fulfilling EP-20 through EP-26.

Purpose: This is the final plan for Phase 5, completing all remaining enterprise portal requirements. The compliance reports provide enterprise users with auditable PoDL trails and GRI-aligned ESG documentation. The settings pages let enterprises manage their organization, team, and operational preferences. These are composition-heavy pages that reuse existing components and patterns.

Output: PoDL/ESG PDF export with @react-pdf/renderer dynamic imports, completed projects archive, payments hub with history and settings, organization profile form, team management DataTable, payment preferences form, notification preferences grid -- with MSW handlers for all endpoints.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enterprise-portal/05-RESEARCH.md
@.planning/phases/05-enterprise-portal/05-01-SUMMARY.md
@.planning/phases/05-enterprise-portal/05-02-SUMMARY.md
@.planning/phases/05-enterprise-portal/05-03-SUMMARY.md
@.planning/phases/05-enterprise-portal/05-05-SUMMARY.md

Key codebase references:
@apps/university-portal/src/components/credentials/podl-pdf-document.tsx (PDF export pattern)
@apps/women-portal/src/app/(app)/settings/notifications/page.tsx (notification preferences pattern)
@packages/types/src/enterprise.ts (ESGReportData, OrganizationProfile, TeamMember, PaymentPreferences)
@packages/types/src/notification.ts (NotificationChannel, NotificationCategory, NotificationPreference)
@apps/enterprise-portal/src/components/shared/otp-confirmation-dialog.tsx (OTPConfirmationDialog built in 05-02)
@.planning/phases/05-enterprise-portal/05-04-SUMMARY.md (Gantt + project-detail-tabs.tsx changes from 05-04)

CRITICAL constraints:
- @react-pdf/renderer MUST be dynamically imported: `const { pdf } = await import('@react-pdf/renderer')` -- never top-level import in page components
- The PDF document component files (podl-report-pdf.tsx, esg-report-pdf.tsx) CAN use top-level imports from @react-pdf/renderer since they are only imported dynamically
- PDF brand colors: #A0614A (terracotta header), #6B4C3B (subtitle/caption), #2C1F1A (body text), #EAD9CC (divider)
- Do NOT barrel-export PDF document components -- they must only be dynamically imported inside event handlers
- @glimmora/ui Label is a <p> NOT a <label> -- use native <label> for forms
- Notification categories for enterprise: sow_updates, project_updates, evidence_reviews, payment_updates, team_activity, platform_announcements
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PoDL audit report PDF, ESG compliance report PDF, export forms, compliance hub, and completed projects archive</name>
  <files>
    apps/enterprise-portal/src/app/(app)/compliance/page.tsx
    apps/enterprise-portal/src/app/(app)/compliance/podl/page.tsx
    apps/enterprise-portal/src/app/(app)/compliance/esg/page.tsx
    apps/enterprise-portal/src/components/compliance/podl-report-pdf.tsx
    apps/enterprise-portal/src/components/compliance/esg-report-pdf.tsx
    apps/enterprise-portal/src/components/compliance/podl-export-form.tsx
    apps/enterprise-portal/src/components/compliance/esg-export-form.tsx
    apps/enterprise-portal/src/components/compliance/index.ts
    apps/enterprise-portal/src/app/(app)/projects/completed/page.tsx
    apps/enterprise-portal/src/lib/msw/handlers/compliance.ts
    apps/enterprise-portal/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create PoDL Audit Report PDF document.**

**`apps/enterprise-portal/src/components/compliance/podl-report-pdf.tsx`:**
This file uses top-level imports from @react-pdf/renderer (it will only be dynamically imported).

```typescript
'use client'
import { Document, Page, Text, View, StyleSheet } from '@react-pdf/renderer'
```

StyleSheet with brand colors:
- page: padding 40, fontFamily 'Helvetica'
- header: fontSize 24, color '#A0614A' (terracotta)
- subtitle: fontSize 12, color '#6B4C3B'
- sectionTitle: fontSize 14, color '#2C1F1A', fontWeight 'bold'
- label: fontSize 10, color '#6B4C3B', textTransform 'uppercase', letterSpacing 1
- value: fontSize 13, color '#2C1F1A'
- divider: height 1, backgroundColor '#EAD9CC'
- brandBar: height 4, backgroundColor '#A0614A', borderRadius 2
- footer: position 'absolute', bottom 40

PoDL Audit Report content:
1. Brand bar
2. Header: "PoDL Audit Report"
3. Subtitle: project name + completion date
4. Section: "Project Summary" -- SOW version, total milestones, total tasks, duration
5. Section: "Milestone Verification" -- for each milestone: name, deliverables verified count, skills demonstrated, quality score
6. Section: "Payment Records" -- for each payment: amount, date, transaction ID, status
7. Section: "Verification" -- verification hashes (mock SHA-256 hashes), cryptographic signature indicator
8. Footer: "Generated by GlimmoraTeam -- Baarez Technology Solutions" + timestamp

Interface for report data: PoDLReportData with projectName, sowVersion, completionDate, milestones[], payments[], verificationHashes[]

Export: `export function PoDLReportPDF({ data }: { data: PoDLReportData })`

**Step 2: Create ESG Compliance Report PDF document.**

**`apps/enterprise-portal/src/components/compliance/esg-report-pdf.tsx`:**

Follow the EXACT code pattern from 05-RESEARCH.md "ESG Compliance Report PDF" code example. Uses ESGReportData from @glimmora/types.

Sections:
1. Brand bar + header + subtitle (organization name + report period)
2. Workforce Diversity (GRI 405): women hours, student hours, percentages
3. Skills Development (GRI 404): PoDL credentials issued, skills developed
4. Fair Payment: total released, on-time rate
5. Delivery Quality: on-time delivery, rework rate, mentor review coverage
6. Footer with generation date and GRI Standards alignment note

Export: `export function ESGReportPDF({ data }: { data: ESGReportData })`

**Step 3: Create PoDL export form.**

**`apps/enterprise-portal/src/components/compliance/podl-export-form.tsx`:**
```
'use client'
```

Form for selecting project and exporting PoDL report:
1. Select project dropdown (fetches GET /api/enterprise/projects/completed for completed projects)
2. Selected project shows summary (name, completion date, milestone count)
3. "Export PoDL Report" button:
   ```typescript
   async function handleExport() {
     setExporting(true)
     try {
       const res = await fetch(`/api/enterprise/compliance/podl/${selectedProjectId}`)
       const { data } = await res.json()

       const { pdf } = await import('@react-pdf/renderer')
       const { PoDLReportPDF } = await import('./podl-report-pdf')

       const blob = await pdf(<PoDLReportPDF data={data} />).toBlob()
       const url = URL.createObjectURL(blob)
       const a = document.createElement('a')
       a.href = url
       a.download = `podl-report-${selectedProjectId}.pdf`
       a.click()
       URL.revokeObjectURL(url)
     } finally {
       setExporting(false)
     }
   }
   ```
4. Loading state with Spinner during export

**Step 4: Create ESG export form.**

**`apps/enterprise-portal/src/components/compliance/esg-export-form.tsx`:**
```
'use client'
```

Form for selecting date range and exporting ESG report:
1. Date range selector: Start Date (DatePicker from @glimmora/ui) and End Date
2. "Export ESG Report" button using same dynamic import pattern as PoDL:
   ```typescript
   const { pdf } = await import('@react-pdf/renderer')
   const { ESGReportPDF } = await import('./esg-report-pdf')
   ```
3. Fetches GET /api/enterprise/compliance/esg?from=...&to=... for report data

**Step 5: Create compliance hub and route pages.**

**`apps/enterprise-portal/src/app/(app)/compliance/page.tsx`:**
Simple page with PageHeader "Compliance & Reports" and cards linking to PoDL and ESG sub-pages.

**`apps/enterprise-portal/src/app/(app)/compliance/podl/page.tsx`:**
PageHeader "PoDL Audit Reports" + PoDLExportForm component.

**`apps/enterprise-portal/src/app/(app)/compliance/esg/page.tsx`:**
PageHeader "ESG Compliance Reports" + ESGExportForm component.

**Step 6: Create completed projects archive.**

**`apps/enterprise-portal/src/app/(app)/projects/completed/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/projects/completed via TanStack useQuery. Renders:
1. PageHeader "Completed Projects"
2. DataTable with columns: Project Name, Completion Date, Total Tasks, Duration, PoDL Report
3. PoDL Report column has "View Report" link -> /compliance/podl (with project pre-selected)
4. Each row is clickable -> /projects/:id for archived detail view

**Step 7: Create MSW compliance handlers.**

**`apps/enterprise-portal/src/lib/msw/handlers/compliance.ts`:**
- GET /api/enterprise/compliance/podl/:projectId -> returns mock PoDL report data with project summary, milestone verifications, payment records, and SHA-256 verification hashes
- GET /api/enterprise/compliance/esg -> returns ESGReportData with realistic numbers: womenContributorHours: 2400, studentContributorHours: 1800, womenWorkforcePercentage: 0.42, podlCredentialsIssued: 34, onTimePaymentRate: 0.96, etc.

**`apps/enterprise-portal/src/components/compliance/index.ts`:**
Export PoDLExportForm and ESGExportForm ONLY (NOT the PDF documents -- those are dynamically imported).

Update **handlers/index.ts** to include compliance handlers.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/enterprise-portal` -- builds without errors.
Verify PoDL PDF: `grep "Document.*Page.*Text.*View" apps/enterprise-portal/src/components/compliance/podl-report-pdf.tsx` -- must match @react-pdf/renderer components.
Verify ESG PDF: `grep "ESGReportPDF\|GRI" apps/enterprise-portal/src/components/compliance/esg-report-pdf.tsx` -- must match.
Verify dynamic import in export form: `grep "await import.*@react-pdf/renderer" apps/enterprise-portal/src/components/compliance/podl-export-form.tsx` -- must match.
Verify NO barrel export of PDF docs: `grep "podl-report-pdf\|esg-report-pdf" apps/enterprise-portal/src/components/compliance/index.ts` -- must return 0 (NOT exported from barrel).
Verify brand colors: `grep "#A0614A" apps/enterprise-portal/src/components/compliance/podl-report-pdf.tsx` -- must match terracotta.
Verify completed projects: `ls apps/enterprise-portal/src/app/\(app\)/projects/completed/page.tsx` -- exists.
Verify compliance handlers: `grep "compliance" apps/enterprise-portal/src/lib/msw/handlers/index.ts` -- must match.
  </verify>
  <done>PoDL audit report PDF with brand-styled sections (project summary, milestone verification, payment records, verification hashes). ESG compliance report PDF with GRI-aligned sections (workforce diversity, skills development, fair payment, delivery quality). Export forms with dynamic @react-pdf/renderer imports. Compliance hub with PoDL and ESG sub-pages. Completed projects archive with PoDL trail links. MSW compliance handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Build payments hub, organization profile, team access management, payment preferences, notification preferences, and settings hub</name>
  <files>
    apps/enterprise-portal/src/app/(app)/payments/page.tsx
    apps/enterprise-portal/src/app/(app)/payments/settings/page.tsx
    apps/enterprise-portal/src/components/payments/payment-history-table.tsx
    apps/enterprise-portal/src/components/payments/pending-approvals-table.tsx
    apps/enterprise-portal/src/components/payments/payment-settings-form.tsx
    apps/enterprise-portal/src/components/payments/index.ts
    apps/enterprise-portal/src/app/(app)/settings/page.tsx
    apps/enterprise-portal/src/app/(app)/settings/organization/page.tsx
    apps/enterprise-portal/src/app/(app)/settings/team/page.tsx
    apps/enterprise-portal/src/app/(app)/settings/notifications/page.tsx
    apps/enterprise-portal/src/lib/msw/handlers/settings.ts
    apps/enterprise-portal/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create payments hub.**

**`apps/enterprise-portal/src/app/(app)/payments/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/payments/history via TanStack useQuery. Renders:
1. PageHeader "Payments" with "Payment Settings" link button -> /payments/settings
2. Tabs (from @glimmora/ui): "Pending" | "History"
3. Pending tab: PendingApprovalsTable showing payments with status 'pending'
4. History tab: PaymentHistoryTable showing all payments

**Step 2: Create payment tables.**

**`apps/enterprise-portal/src/components/payments/payment-history-table.tsx`:**
DataTable<PaymentRecord> with columns: Project, Milestone, Amount, Platform Fee, Net Amount, Status (Badge), Release Mode, Released Date, Transaction ID.

**`apps/enterprise-portal/src/components/payments/pending-approvals-table.tsx`:**
DataTable<PaymentRecord> filtered to status='pending'. Columns: Project, Milestone, Amount, Evidence Pack, Actions (Release button). Release button opens OTPConfirmationDialog.
Import: `import { OTPConfirmationDialog } from '../shared/otp-confirmation-dialog'`

**Step 3: Create payment settings page.**

**`apps/enterprise-portal/src/app/(app)/payments/settings/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/payments/preferences. Renders PaymentSettingsForm.

**`apps/enterprise-portal/src/components/payments/payment-settings-form.tsx`:**
```
'use client'
```

Form with:
1. Default Release Mode: RadioGroup with 3 options:
   - Manual: "Review and approve each payment individually with OTP confirmation"
   - Auto on Approval: "Automatically release payment when evidence is approved"
   - APG Silent: "APG automatically approves and releases payments below threshold"
2. APG Silent Threshold: TextInput (number, only visible when APG Silent mode selected) with native `<label>`
3. Auto-Release Delay: TextInput (number of days, only visible when Auto mode selected) with native `<label>`
4. "Save Preferences" button calls PATCH /api/enterprise/payments/preferences

**Step 4: Create settings hub and pages.**

**`apps/enterprise-portal/src/app/(app)/settings/page.tsx`:**
Settings hub with sidebar navigation linking to sub-routes. Same pattern as women-portal settings root page:
- Organization Profile -> /settings/organization
- Team Access -> /settings/team
- Notifications -> /settings/notifications

**Step 5: Create organization profile page.**

**`apps/enterprise-portal/src/app/(app)/settings/organization/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/organization. Renders form with:
1. Company Name (TextInput)
2. Logo URL (TextInput, with preview if URL is set)
3. Industry (Select)
4. Company Size (Select: 1-50, 51-200, 201-1000, 1000+)
5. Headquarters (TextInput)
6. Website (TextInput, optional)
7. Primary Contact Email (TextInput)
8. Tax ID (TextInput, read-only after verification)
9. Verification Status display (Badge: pending/verified/rejected)
10. "Save Changes" button calls PATCH /api/enterprise/organization
All form labels use native `<label>` elements.

**Step 6: Create team access management page.**

**`apps/enterprise-portal/src/app/(app)/settings/team/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/team. Renders:
1. PageHeader "Team Access" with "Invite Member" button
2. DataTable<TeamMember> with columns:
   - Name
   - Email
   - Role (Badge or Select for editing)
   - Status (Badge: active/pending invitation)
   - Invited Date
   - Actions: Change Role (Select dropdown), Remove (destructive button with confirmation Dialog)
3. "Invite Member" button opens Dialog with Email + Role (Select) form
   - Calls POST /api/enterprise/team/invite
4. Role change calls PATCH /api/enterprise/team/:id
5. Remove calls DELETE /api/enterprise/team/:id

**Step 7: Create notification preferences page.**

**`apps/enterprise-portal/src/app/(app)/settings/notifications/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/notifications/preferences. Renders:

Grid of notification categories x channels (same pattern as women-portal notification preferences):

Enterprise notification categories:
- SOW Updates (sow_updates)
- Project Updates (project_updates)
- Evidence Reviews (evidence_reviews)
- Payment Updates (payment_updates)
- Team Activity (team_activity)
- Platform Announcements (platform_announcements)

Channels: In-App, Email

Each cell is a Switch toggle from @glimmora/ui. Save button calls PATCH /api/enterprise/notifications/preferences.

**Step 8: Create MSW settings handlers.**

**`apps/enterprise-portal/src/lib/msw/handlers/settings.ts`:**
- GET /api/enterprise/organization -> returns mock OrganizationProfile
- PATCH /api/enterprise/organization -> 200
- GET /api/enterprise/team -> returns 4 mock TeamMembers (admin, PM, finance, viewer)
- POST /api/enterprise/team/invite -> 201, returns new TeamMember
- PATCH /api/enterprise/team/:id -> 200
- DELETE /api/enterprise/team/:id -> 204
- GET /api/enterprise/notifications/preferences -> returns notification preference grid
- PATCH /api/enterprise/notifications/preferences -> 200

**`apps/enterprise-portal/src/components/payments/index.ts`:**
Barrel export payment components.

Update **handlers/index.ts** to include settings handlers.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/enterprise-portal` -- builds without errors.
Verify payments hub: `ls apps/enterprise-portal/src/app/\(app\)/payments/{page.tsx,settings/page.tsx}` -- both exist.
Verify org settings: `ls apps/enterprise-portal/src/app/\(app\)/settings/{page.tsx,organization/page.tsx,team/page.tsx,notifications/page.tsx}` -- all exist.
Verify team DataTable: `grep "DataTable\|TeamMember" apps/enterprise-portal/src/app/\(app\)/settings/team/page.tsx` -- must match.
Verify payment preferences: `grep "PaymentReleaseMode\|manual\|auto-on-approval\|apg-silent" apps/enterprise-portal/src/components/payments/payment-settings-form.tsx` -- must match.
Verify notification Switch: `grep "Switch" apps/enterprise-portal/src/app/\(app\)/settings/notifications/page.tsx` -- must match.
Verify native labels: `grep "<label" apps/enterprise-portal/src/app/\(app\)/settings/organization/page.tsx` -- must match.
Verify settings handlers: `grep "settings" apps/enterprise-portal/src/lib/msw/handlers/index.ts` -- must match.
  </verify>
  <done>Payments hub with pending/history tabs and OTP-enabled release. Payment settings with mode selector (manual/auto/apg-silent) and threshold. Organization profile form with all fields and verification status. Team access DataTable with invite/role-change/remove. Notification preferences grid with Switch toggles. Settings hub with sub-route navigation. MSW settings handlers for all endpoints.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/enterprise-portal` builds without errors
2. PoDL PDF uses @react-pdf/renderer with brand colors (#A0614A, #6B4C3B, #2C1F1A, #EAD9CC)
3. ESG PDF renders GRI-aligned sections (Workforce Diversity, Skills Development, Fair Payment, Delivery Quality)
4. PDF documents are dynamically imported (not barrel-exported)
5. Completed projects archive shows finished projects with PoDL trail
6. Payments hub has pending/history tabs
7. Payment settings allow manual/auto/apg-silent mode selection
8. Organization profile has all fields with verification status display
9. Team management DataTable with invite/role/remove actions
10. Notification preferences grid with Switch toggles for 6 categories x 2 channels
11. MSW handlers for compliance, settings (org, team, notifications) endpoints
</verification>

<success_criteria>
- Enterprise user can export a PoDL audit report as a branded PDF for any completed project
- Enterprise user can export an ESG compliance report as a branded PDF with GRI-aligned metrics
- Enterprise user can view completed projects with PoDL trail links
- Enterprise user can manage organization profile, team access, payment preferences, and notification preferences
- All PDF exports use dynamic imports and never cause SSR crashes
- All 26 EP requirements (EP-01 through EP-26) are covered across Plans 05-01 through 05-06
</success_criteria>

<output>
After completion, create `.planning/phases/05-enterprise-portal/05-06-SUMMARY.md`
</output>

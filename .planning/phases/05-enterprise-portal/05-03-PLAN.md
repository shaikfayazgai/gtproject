---
phase: 05-enterprise-portal
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/enterprise-portal/src/app/(app)/dashboard/page.tsx
  - apps/enterprise-portal/src/components/dashboard/active-projects-widget.tsx
  - apps/enterprise-portal/src/components/dashboard/pending-actions-widget.tsx
  - apps/enterprise-portal/src/components/dashboard/budget-widget.tsx
  - apps/enterprise-portal/src/components/dashboard/health-metrics-widget.tsx
  - apps/enterprise-portal/src/components/dashboard/index.ts
  - apps/enterprise-portal/src/app/(app)/projects/page.tsx
  - apps/enterprise-portal/src/app/(app)/projects/[projectId]/page.tsx
  - apps/enterprise-portal/src/components/projects/project-card.tsx
  - apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx
  - apps/enterprise-portal/src/components/projects/project-overview.tsx
  - apps/enterprise-portal/src/components/projects/team-summary-grid.tsx
  - apps/enterprise-portal/src/components/projects/index.ts
  - apps/enterprise-portal/src/lib/msw/factories/project.ts
  - apps/enterprise-portal/src/lib/msw/handlers/projects.ts
  - apps/enterprise-portal/src/lib/msw/handlers/dashboard.ts
  - apps/enterprise-portal/src/lib/msw/handlers/index.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard shows 4 GradientCard KPIs: tasks complete %, evidence packs pending, payments released vs pending, timeline health"
    - "3rd GradientCard uses inline style gradient (forest-to-teal) per prior decision"
    - "Dashboard shows APG Activity Feed widget using APGFeed component from @glimmora/ui"
    - "Projects list page shows active projects as ProjectCards with status, health indicator, completion percentage"
    - "Project detail page uses client-side Tabs component with 7 tabs: Overview, Timeline, Evidence Packs, Rework Requests, Escalation Centre, Payment Release, Team Summary"
    - "Tab state is reflected in URL hash for bookmarkability"
    - "Overview tab shows project summary with milestones progress, budget usage, and APG activity"
    - "Team Summary tab shows anonymized contributor cards using AnonymizedTeamCard from @glimmora/ui"
  artifacts:
    - path: "apps/enterprise-portal/src/app/(app)/dashboard/page.tsx"
      provides: "Enterprise dashboard with KPIs and APG feed (EP-06, EP-07)"
    - path: "apps/enterprise-portal/src/app/(app)/projects/page.tsx"
      provides: "Active projects list"
    - path: "apps/enterprise-portal/src/app/(app)/projects/[projectId]/page.tsx"
      provides: "Project detail with 7-tab layout (EP-08)"
    - path: "apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx"
      provides: "7-tab project detail component"
      contains: "ProjectDetailTabs"
    - path: "apps/enterprise-portal/src/components/projects/team-summary-grid.tsx"
      provides: "Anonymized team display using AnonymizedTeamCard"
  key_links:
    - from: "apps/enterprise-portal/src/app/(app)/dashboard/page.tsx"
      to: "@glimmora/ui GradientCard"
      via: "4 KPI cards with gradient variants"
      pattern: "import.*GradientCard.*from.*@glimmora/ui"
    - from: "apps/enterprise-portal/src/app/(app)/dashboard/page.tsx"
      to: "@glimmora/ui APGFeed"
      via: "APG activity feed widget"
      pattern: "import.*APGFeed.*from.*@glimmora/ui"
    - from: "apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx"
      to: "@glimmora/ui Tabs"
      via: "7-tab layout with hash-based state"
      pattern: "import.*Tabs.*TabsList.*TabsTrigger.*TabsContent.*from.*@glimmora/ui"
    - from: "apps/enterprise-portal/src/components/projects/team-summary-grid.tsx"
      to: "@glimmora/ui AnonymizedTeamCard"
      via: "Anonymized contributor display (no real names)"
      pattern: "import.*AnonymizedTeamCard.*from.*@glimmora/ui"
    - from: "apps/enterprise-portal/src/lib/msw/handlers/dashboard.ts"
      to: "@glimmora/types APGActivity"
      via: "recentActivity array in dashboard response must match APGActivity[] interface"
      pattern: "APGActivity"
---

<objective>
Build the enterprise dashboard with GradientCard KPIs and APG activity feed, the active projects list, and the 7-tab project detail page with Overview and Team Summary tabs -- fulfilling EP-06, EP-07, and EP-08.

Purpose: The dashboard is the enterprise user's landing page after login -- it surfaces the most critical metrics (completion, pending evidence, payments, timeline health) at a glance. The project detail page with 7 tabs is the hub for all project-level operations. This plan builds the container (tabs) and the information-focused tabs (Overview, Team Summary), while the action-oriented tabs (Timeline, Evidence, Rework, Escalation, Payments) are populated by Plans 05-04 and 05-05.

Output: Enterprise dashboard with 4 GradientCard KPIs + APG feed, project list with cards, 7-tab project detail (Overview + Team Summary fully functional, other 5 tabs as placeholder content for Plans 05-04 and 05-05 to fill), and MSW handlers for dashboard/projects.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enterprise-portal/05-RESEARCH.md
@.planning/phases/05-enterprise-portal/05-01-SUMMARY.md

Key codebase references:
@packages/ui/src/components/gradient-card/gradient-card.tsx (two variants: primary, nature)
@packages/ui/src/components/apg-feed/apg-feed.tsx (APGFeed component with APGActivity items)
@packages/ui/src/components/tabs/tabs.tsx (Tabs, TabsList, TabsTrigger, TabsContent)
@packages/ui/src/components/anonymized-team-card/anonymized-team-card.tsx
@packages/types/src/project.ts (Project, ProjectMilestone)
@packages/types/src/api.ts (APGActivity)

CRITICAL constraints:
- GradientCard has two variants: 'primary' (terracotta to gold) and 'nature' (forest to teal). For 4 KPI cards: card 1 primary, card 2 primary, card 3 uses inline style={{ background: 'linear-gradient(135deg, #4A6741 0%, #3A8FA0 100%)' }}, card 4 nature variant
- APGFeed expects APGActivity[] with action types: task_completed, evidence_submitted, payment_triggered, skill_verified, team_formed, milestone_reached, etc.
- Project detail uses client-side Tabs (single route /projects/[projectId]/page.tsx) NOT separate routes per tab
- AnonymizedTeamCard shows anonymous seed, skills (max 4 + overflow), tier badge -- NO real contributor names
- Do NOT create separate route files for Timeline, Evidence, etc. -- those tabs are rendered inside the same page component. Plans 05-04 and 05-05 will create components that replace the placeholder content.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build enterprise dashboard with GradientCard KPIs and APG activity feed</name>
  <files>
    apps/enterprise-portal/src/app/(app)/dashboard/page.tsx
    apps/enterprise-portal/src/components/dashboard/active-projects-widget.tsx
    apps/enterprise-portal/src/components/dashboard/pending-actions-widget.tsx
    apps/enterprise-portal/src/components/dashboard/budget-widget.tsx
    apps/enterprise-portal/src/components/dashboard/health-metrics-widget.tsx
    apps/enterprise-portal/src/components/dashboard/index.ts
    apps/enterprise-portal/src/lib/msw/handlers/dashboard.ts
    apps/enterprise-portal/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create dashboard page.**

**`apps/enterprise-portal/src/app/(app)/dashboard/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/dashboard via TanStack useQuery. Renders:

1. PageHeader with "Dashboard" title
2. **4 GradientCard KPIs in a grid** (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4):
   - Card 1 (variant="primary"): "Tasks Complete" -- percentage (e.g., "72%"), subtitle "across 3 active projects"
   - Card 2 (variant="primary"): "Evidence Packs Pending" -- count (e.g., "8"), subtitle "awaiting your review"
   - Card 3 (inline style gradient): "Payments Released" -- amount (e.g., "$142,500"), subtitle "of $250,000 total"
     - `style={{ background: 'linear-gradient(135deg, #4A6741 0%, #3A8FA0 100%)' }}`
   - Card 4 (variant="nature"): "Timeline Health" -- status text (e.g., "On Track"), subtitle "2 milestones this week"
3. **Two-column layout** below KPIs (lg:grid-cols-2):
   - Left: ActiveProjectsWidget (list of active projects with status, click to navigate)
   - Right: APG Activity Feed using APGFeed from @glimmora/ui with mock APGActivity items

**Step 2: Create dashboard widgets.**

**`apps/enterprise-portal/src/components/dashboard/active-projects-widget.tsx`:**
Card with "Active Projects" heading. Lists 3-4 projects with name, status Badge, completion Progress bar, health indicator dot (green/amber/red). Each project is clickable -> /projects/:id.

**`apps/enterprise-portal/src/components/dashboard/pending-actions-widget.tsx`:**
Card with "Pending Actions" heading. Lists actionable items: "3 evidence packs to review", "2 payments awaiting release", "1 blueprint awaiting approval". Each item is a link to the relevant page.

**`apps/enterprise-portal/src/components/dashboard/budget-widget.tsx`:**
Card with "Budget Overview" heading. Shows total budget, released amount, pending amount with a simple bar chart or Progress component.

**`apps/enterprise-portal/src/components/dashboard/health-metrics-widget.tsx`:**
Card with "Health Metrics" heading. Shows on-time delivery rate, rework rate, average review time as simple stat rows.

**`apps/enterprise-portal/src/components/dashboard/index.ts`:**
Barrel export all dashboard widgets.

**Step 3: Create MSW dashboard handler.**

**`apps/enterprise-portal/src/lib/msw/handlers/dashboard.ts`:**
- GET /api/enterprise/dashboard -> returns mock summary:
  ```json
  {
    "data": {
      "tasksCompletePercentage": 72,
      "activeProjectCount": 3,
      "evidencePacksPending": 8,
      "paymentsReleased": 142500,
      "paymentsTotal": 250000,
      "currency": "USD",
      "timelineHealth": "on-track",
      "milestonesThisWeek": 2,
      "activeProjects": [...],
      "recentActivity": [APGActivity items],
      "pendingActions": [...]
    }
  }
  ```

The recentActivity array contains 5-6 APGActivity items with realistic enterprise actions:
- team_formed, milestone_reached, evidence_submitted, payment_triggered, task_completed
- Each with timestamp, description, and optional metadata

Create/update **handlers/index.ts** to include dashboard handlers:
```typescript
import { dashboardHandlers } from './dashboard'
export const handlers = [...dashboardHandlers]
```
Task 2 will append project handlers to this list.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/enterprise-portal` -- builds without errors.
Verify dashboard page: `ls apps/enterprise-portal/src/app/\(app\)/dashboard/page.tsx` -- exists.
Verify GradientCard usage: `grep "GradientCard" apps/enterprise-portal/src/app/\(app\)/dashboard/page.tsx` -- must match.
Verify inline gradient: `grep "4A6741.*3A8FA0\|linear-gradient" apps/enterprise-portal/src/app/\(app\)/dashboard/page.tsx` -- must match (3rd card inline gradient).
Verify APGFeed: `grep "APGFeed" apps/enterprise-portal/src/app/\(app\)/dashboard/page.tsx` -- must match.
  </verify>
  <done>Enterprise dashboard with 4 GradientCard KPIs (primary/primary/inline-gradient/nature variants), active projects widget, pending actions widget, APG Activity Feed. MSW dashboard handler returns comprehensive mock summary data.</done>
</task>

<task type="auto">
  <name>Task 2: Build projects list, 7-tab project detail (Overview + Team Summary), and MSW project handlers</name>
  <files>
    apps/enterprise-portal/src/app/(app)/projects/page.tsx
    apps/enterprise-portal/src/app/(app)/projects/[projectId]/page.tsx
    apps/enterprise-portal/src/components/projects/project-card.tsx
    apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx
    apps/enterprise-portal/src/components/projects/project-overview.tsx
    apps/enterprise-portal/src/components/projects/team-summary-grid.tsx
    apps/enterprise-portal/src/components/projects/index.ts
    apps/enterprise-portal/src/lib/msw/factories/project.ts
    apps/enterprise-portal/src/lib/msw/handlers/projects.ts
    apps/enterprise-portal/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create projects list page.**

**`apps/enterprise-portal/src/app/(app)/projects/page.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/projects via TanStack useQuery. Renders:
1. PageHeader "Active Projects" with "Upload New SOW" button -> /sow/upload
2. Grid of ProjectCard components (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)
3. Empty state if no projects

**Step 2: Create ProjectCard.**

**`apps/enterprise-portal/src/components/projects/project-card.tsx`:**
```
'use client'
```

Card showing project summary:
- Project name (heading)
- Status Badge (active, paused, blueprint-review)
- Health indicator dot + text (on-track = green, at-risk = amber, delayed = red, critical = red pulse)
- Completion Progress bar with percentage
- Task stats: "X/Y tasks complete"
- Timeline: start date - target end date
- Click navigates to /projects/:id

Props: `{ project: Project }`

**Step 3: Create project detail page with 7-tab layout.**

**`apps/enterprise-portal/src/app/(app)/projects/[projectId]/page.tsx`:**
```
'use client'
```

1. Reads projectId from URL params
2. Fetches GET /api/enterprise/projects/:id via TanStack useQuery
3. Shows loading spinner while fetching
4. Renders PageHeader with project name, status badge, health indicator
5. Renders ProjectDetailTabs component

**Step 4: Create ProjectDetailTabs.**

**`apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx`:**
```
'use client'
```

Uses Tabs from @glimmora/ui with 7 tabs. Tab state is stored in URL hash for bookmarkability:

```typescript
'use client'
import { useState, useEffect } from 'react'
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@glimmora/ui'

const TABS = [
  { value: 'overview', label: 'Overview' },
  { value: 'timeline', label: 'Timeline' },
  { value: 'evidence', label: 'Evidence Packs' },
  { value: 'rework', label: 'Rework Requests' },
  { value: 'escalation', label: 'Escalation Centre' },
  { value: 'payments', label: 'Payment Release' },
  { value: 'team', label: 'Team Summary' },
] as const
```

Read initial tab from `window.location.hash` (remove #). Default to 'overview' if no hash.
On tab change, update `window.location.hash` without navigation.

Tab contents:
- **overview**: ProjectOverview component (fully implemented)
- **timeline**: Placeholder text "Timeline view will be added" (Plan 05-04 fills this)
- **evidence**: Placeholder text "Evidence packs view will be added" (Plan 05-05 fills this)
- **rework**: Placeholder text "Rework requests view will be added" (Plan 05-05 fills this)
- **escalation**: Placeholder text "Escalation centre will be added" (Plan 05-05 fills this)
- **payments**: Placeholder text "Payment release view will be added" (Plan 05-05 fills this)
- **team**: TeamSummaryGrid component (fully implemented)

Props: `{ project: Project; projectId: string }`

**Step 5: Create ProjectOverview.**

**`apps/enterprise-portal/src/components/projects/project-overview.tsx`:**
```
'use client'
```

Fetches additional data for the overview tab:
- GET /api/enterprise/projects/:id/apg-activity for APG feed
- GET /api/enterprise/projects/:id/timeline for milestone summary

Renders:
1. Summary stats: total tasks, completed tasks, budget used vs total, days remaining
2. Milestone progress section: list of milestones with status indicators and completion
3. APG Activity Feed (APGFeed component from @glimmora/ui) for this project
4. Budget breakdown: visual bar showing released vs pending vs remaining

Props: `{ projectId: string; project: Project }`

**Step 6: Create TeamSummaryGrid.**

**`apps/enterprise-portal/src/components/projects/team-summary-grid.tsx`:**
```
'use client'
```

Fetches GET /api/enterprise/projects/:id/team via TanStack useQuery. Renders:
1. Heading "Team Members" with count
2. Grid of AnonymizedTeamCard components from @glimmora/ui (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3)
3. Each card shows: anonymous seed (for deterministic avatar), skill tags (max 4 + overflow), tier badge, tasks assigned count
4. NO real contributor names visible anywhere -- this is the privacy boundary

Props: `{ projectId: string }`

**Step 7: Create MSW project factories and handlers.**

**`apps/enterprise-portal/src/lib/msw/factories/project.ts`:**
- `createMockProject(id: string)` -- returns Project with realistic data
- `createMockProjectsList()` -- returns 4 projects with different statuses/health
- `createMockProjectMilestones(projectId: string)` -- returns 5 milestones with mixed statuses
- `createMockTeamMembers()` -- returns 6-8 anonymized team member objects (seed, skills, tier, taskCount)
- `createMockAPGActivity(projectId: string)` -- returns 6-8 APGActivity items

**`apps/enterprise-portal/src/lib/msw/handlers/projects.ts`:**
- GET /api/enterprise/projects -> returns createMockProjectsList()
- GET /api/enterprise/projects/:id -> returns createMockProject(id)
- GET /api/enterprise/projects/:id/timeline -> returns createMockProjectMilestones(id)
- GET /api/enterprise/projects/:id/apg-activity -> returns createMockAPGActivity(id)
- GET /api/enterprise/projects/:id/team -> returns createMockTeamMembers()
- PATCH /api/enterprise/projects/:id/hold -> 200
- GET /api/enterprise/projects/completed -> returns 2 completed projects

Update **handlers/index.ts** to include BOTH dashboard handlers (created in Task 1) AND projects handlers:
```typescript
import { dashboardHandlers } from './dashboard'
import { projectHandlers } from './projects'
export const handlers = [...dashboardHandlers, ...projectHandlers]
```

**Barrel export:**
**`apps/enterprise-portal/src/components/projects/index.ts`** exports all project components.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/enterprise-portal` -- builds without errors.
Verify projects list: `ls apps/enterprise-portal/src/app/\(app\)/projects/page.tsx` -- exists.
Verify project detail: `ls apps/enterprise-portal/src/app/\(app\)/projects/\[projectId\]/page.tsx` -- exists.
Verify 7 tabs: `grep "overview.*timeline.*evidence.*rework.*escalation.*payments.*team" apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx` -- must match all 7 tab values.
Verify Tabs import: `grep "import.*Tabs.*TabsList.*TabsTrigger.*TabsContent" apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx` -- must match.
Verify hash state: `grep "location.hash\|window.location" apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx` -- must match hash-based tab state.
Verify AnonymizedTeamCard: `grep "AnonymizedTeamCard" apps/enterprise-portal/src/components/projects/team-summary-grid.tsx` -- must match.
Verify APGFeed in overview: `grep "APGFeed" apps/enterprise-portal/src/components/projects/project-overview.tsx` -- must match.
Verify projects handler: `grep "projects" apps/enterprise-portal/src/lib/msw/handlers/index.ts` -- must match.
  </verify>
  <done>Projects list with ProjectCard components showing status/health/completion. Project detail with 7-tab layout (hash-based tab state). Overview tab with milestone progress, budget summary, and APG Activity Feed. Team Summary tab with AnonymizedTeamCard grid. MSW project handlers for list, detail, timeline, activity, and team endpoints. Placeholder content in Timeline/Evidence/Rework/Escalation/Payments tabs for Plans 05-04 and 05-05.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/enterprise-portal` builds without errors
2. Dashboard has 4 GradientCard KPIs with correct gradient variants (primary, primary, inline forest-teal, nature)
3. Dashboard shows APG Activity Feed using APGFeed from @glimmora/ui
4. Projects list renders ProjectCards with status, health, completion
5. Project detail has 7 client-side tabs (NOT separate routes)
6. Tab state reflected in URL hash (#overview, #timeline, etc.)
7. Overview tab shows milestones, budget, APG feed
8. Team Summary tab shows anonymized contributors via AnonymizedTeamCard (no real names)
9. All 5 placeholder tabs render text indicating future Plans 05-04/05-05 will populate them
10. MSW handlers serve dashboard summary, project list, project detail, milestones, APG activity, and team data
</verification>

<success_criteria>
- Enterprise user lands on dashboard and sees 4 gradient KPI cards with at-a-glance metrics
- Enterprise user can browse active projects and click into a project
- Project detail shows 7 tabs with Overview and Team Summary fully functional
- Timeline/Evidence/Rework/Escalation/Payments tabs have placeholder content ready for subsequent plans
- No contributor names visible anywhere in the Team Summary tab
</success_criteria>

<output>
After completion, create `.planning/phases/05-enterprise-portal/05-03-SUMMARY.md`
</output>

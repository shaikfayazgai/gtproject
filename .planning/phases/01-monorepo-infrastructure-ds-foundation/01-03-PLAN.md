---
phase: 01-monorepo-infrastructure-ds-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/index.ts
  - packages/ui/src/lib/utils.ts
  - packages/ui/src/components/button/button.tsx
  - packages/ui/src/components/button/button.stories.tsx
  - packages/ui/src/components/button/index.ts
  - packages/ui/src/components/input/input.tsx
  - packages/ui/src/components/input/input.stories.tsx
  - packages/ui/src/components/input/index.ts
  - packages/ui/src/components/select/select.tsx
  - packages/ui/src/components/select/select.stories.tsx
  - packages/ui/src/components/select/index.ts
  - packages/ui/src/components/checkbox/checkbox.tsx
  - packages/ui/src/components/checkbox/checkbox.stories.tsx
  - packages/ui/src/components/checkbox/index.ts
  - packages/ui/src/components/radio/radio.tsx
  - packages/ui/src/components/radio/radio.stories.tsx
  - packages/ui/src/components/radio/index.ts
  - packages/ui/src/components/switch/switch.tsx
  - packages/ui/src/components/switch/switch.stories.tsx
  - packages/ui/src/components/switch/index.ts
  - packages/ui/src/components/dialog/dialog.tsx
  - packages/ui/src/components/dialog/dialog.stories.tsx
  - packages/ui/src/components/dialog/index.ts
  - packages/ui/src/components/tooltip/tooltip.tsx
  - packages/ui/src/components/tooltip/tooltip.stories.tsx
  - packages/ui/src/components/tooltip/index.ts
  - packages/ui/src/components/typography/typography.tsx
  - packages/ui/src/components/typography/typography.stories.tsx
  - packages/ui/src/components/typography/index.ts
  - packages/ui/.storybook/main.ts
  - packages/ui/.storybook/preview.ts
  - packages/ui/src/styles/storybook-fonts.css
autonomous: true

must_haves:
  truths:
    - "Storybook launches at port 6006 and renders all foundational component stories"
    - "Button renders in Primary, Secondary, Ghost, Destructive variants with correct brand colors"
    - "All Radix-based components (Select, Checkbox, Radio, Switch, Dialog, Tooltip) render and function interactively in Storybook"
    - "Typography components use font-display for headings and font-body for body text"
    - "Each component file has 'use client' directive at the top"
  artifacts:
    - path: "packages/ui/src/index.ts"
      provides: "Barrel export of all UI components"
      exports: ["Button", "Input", "Select", "Checkbox", "Radio", "Switch", "Dialog", "Tooltip", "Heading", "Body", "Label", "Caption"]
    - path: "packages/ui/src/components/button/button.tsx"
      provides: "Button component with CVA variants"
      contains: "use client"
      min_lines: 40
    - path: "packages/ui/.storybook/main.ts"
      provides: "Storybook 10 ESM config"
      contains: "StorybookConfig"
    - path: "packages/ui/src/components/dialog/dialog.tsx"
      provides: "Radix Dialog with motion animation"
      contains: "Dialog"
  key_links:
    - from: "packages/ui/src/components/button/button.tsx"
      to: "class-variance-authority"
      via: "cva import"
      pattern: "import.*cva.*class-variance-authority"
    - from: "packages/ui/src/components/dialog/dialog.tsx"
      to: "radix-ui"
      via: "Radix Dialog primitive import"
      pattern: "import.*Dialog.*radix-ui"
    - from: "packages/ui/.storybook/preview.ts"
      to: "@glimmora/config/tailwind/theme.css"
      via: "CSS import for design tokens"
      pattern: "@glimmora/config/tailwind/theme.css"
---

<objective>
Build the @glimmora/ui package with all DS-01 through DS-10 foundational design system components (Typography, Button, Input, Select, Checkbox, Radio, Switch, Dialog, Tooltip) using Radix UI primitives + CVA + Tailwind v4, and configure Storybook 10 with stories for every component.

Purpose: Create the shared component library that all 5 portals will consume. Every UI element across the platform uses these primitives. Storybook serves as both the development environment and the backend handoff artifact.

Output: @glimmora/ui exports 12+ components. Storybook runs at port 6006 with stories demonstrating all variants. Components use warm-earth design tokens from @glimmora/config.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-infrastructure-ds-foundation/01-RESEARCH.md

Prior plan 01-01 created the monorepo with @glimmora/config (including Tailwind theme) and a placeholder @glimmora/ui package. This plan replaces the @glimmora/ui placeholder with the full design system.

This plan runs in PARALLEL with 01-02 (Wave 2). It does NOT depend on @glimmora/types.
</context>

<tasks>

<task type="auto">
  <name>Task 1: @glimmora/ui package setup + Storybook 10 config + utility functions</name>
  <files>
    packages/ui/package.json
    packages/ui/tsconfig.json
    packages/ui/src/index.ts
    packages/ui/src/lib/utils.ts
    packages/ui/.storybook/main.ts
    packages/ui/.storybook/preview.ts
    packages/ui/src/styles/storybook-fonts.css
    packages/ui/public/fonts/.gitkeep
  </files>
  <action>
    **1. Update packages/ui/package.json** (replace placeholder):
    ```json
    {
      "name": "@glimmora/ui",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "main": "./src/index.ts",
      "types": "./src/index.ts",
      "exports": {
        ".": {
          "import": "./src/index.ts",
          "types": "./src/index.ts"
        }
      },
      "scripts": {
        "storybook": "storybook dev -p 6006",
        "build-storybook": "storybook build",
        "type-check": "tsc --noEmit",
        "clean": "rm -rf storybook-static .turbo"
      },
      "dependencies": {
        "@glimmora/config": "workspace:*",
        "radix-ui": "^1.4.3",
        "class-variance-authority": "^0.7.1",
        "clsx": "^2.1.1",
        "tailwind-merge": "^3.5.0",
        "motion": "^12.34.3",
        "lucide-react": "^0.575.0",
        "react": "^19.2.4",
        "react-dom": "^19.2.4"
      },
      "devDependencies": {
        "typescript": "^5.9.3",
        "@types/react": "^19.1.8",
        "@types/react-dom": "^19.1.6",
        "@tailwindcss/postcss": "^4.2.1",
        "postcss": "^8.5.5",
        "storybook": "^10.2.12",
        "@storybook/nextjs": "^10.2.12",
        "@storybook/addon-a11y": "^10.2.12"
      }
    }
    ```

    **2. packages/ui/tsconfig.json:**
    ```json
    {
      "extends": "@glimmora/config/typescript/react-library.json",
      "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src",
        "noEmit": true
      },
      "include": ["src/**/*.ts", "src/**/*.tsx"],
      "exclude": ["node_modules", "dist", "storybook-static"]
    }
    ```

    **3. packages/ui/postcss.config.mjs:**
    ```js
    export default {
      plugins: {
        '@tailwindcss/postcss': {}
      }
    }
    ```

    **4. Utility function** `packages/ui/src/lib/utils.ts`:
    ```typescript
    import { clsx, type ClassValue } from 'clsx'
    import { twMerge } from 'tailwind-merge'

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    **5. Storybook config** `packages/ui/.storybook/main.ts`:
    ```typescript
    import type { StorybookConfig } from '@storybook/nextjs'

    const config: StorybookConfig = {
      stories: ['../src/**/*.stories.@(ts|tsx)'],
      framework: {
        name: '@storybook/nextjs',
        options: {},
      },
      addons: ['@storybook/addon-a11y'],
      staticDirs: ['../public'],
    }
    export default config
    ```

    IMPORTANT NOTES on Storybook 10:
    - NO `@storybook/addon-essentials` -- essentials (controls, actions, docs, viewport) are BUILT IN to Storybook 10
    - Only add `@storybook/addon-a11y` explicitly as it is NOT built-in
    - ESM-only -- use `export default`, not `module.exports`

    **6. Storybook preview** `packages/ui/.storybook/preview.ts`:
    ```typescript
    import type { Preview } from '@storybook/nextjs'
    import '@glimmora/config/tailwind/theme.css'
    import '../src/styles/storybook-fonts.css'

    const preview: Preview = {
      parameters: {
        backgrounds: {
          default: 'app',
          values: [
            { name: 'app', value: '#FAF7F4' },
            { name: 'dashboard', value: '#F5EDE6' },
            { name: 'card', value: '#FFFFFF' },
          ],
        },
      },
    }
    export default preview
    ```

    **7. Font CSS for Storybook** `packages/ui/src/styles/storybook-fonts.css`:
    ```css
    /*
     * Storybook font loading
     * Font files should be placed in packages/ui/public/fonts/
     * Until WOFF2 files are available, fallback fonts (Georgia, system-ui) will be used.
     *
     * Uncomment below when font files are placed:
     */

    /*
    @font-face {
      font-family: 'Miller Display';
      src: url('/fonts/MillerDisplay-Light.woff2') format('woff2');
      font-weight: 300;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Miller Display';
      src: url('/fonts/MillerDisplay-Medium.woff2') format('woff2');
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Miller Display';
      src: url('/fonts/MillerDisplay-Black.woff2') format('woff2');
      font-weight: 900;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: 'Avenir LT Std';
      src: url('/fonts/AvenirLTStd-Light.woff2') format('woff2');
      font-weight: 300;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Avenir LT Std';
      src: url('/fonts/AvenirLTStd-Roman.woff2') format('woff2');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Avenir LT Std';
      src: url('/fonts/AvenirLTStd-Medium.woff2') format('woff2');
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Avenir LT Std';
      src: url('/fonts/AvenirLTStd-Heavy.woff2') format('woff2');
      font-weight: 800;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Avenir LT Std';
      src: url('/fonts/AvenirLTStd-Black.woff2') format('woff2');
      font-weight: 900;
      font-style: normal;
      font-display: swap;
    }
    */
    ```

    Create `packages/ui/public/fonts/.gitkeep` as placeholder for font files.

    Run `pnpm install` from monorepo root after creating all files.

    **8. Initialize barrel export** `packages/ui/src/index.ts` (will be populated in Task 2):
    ```typescript
    // Design system components - barrel export
    // NOTE: This file does NOT have "use client" — individual components do.
    // This allows server components to import layout primitives without forcing client mode.

    export { cn } from './lib/utils'
    ```
  </action>
  <verify>
    1. `pnpm install` succeeds
    2. `pnpm --filter @glimmora/ui exec tsc --noEmit` -- type check passes
    3. `cat packages/ui/.storybook/main.ts` -- correct ESM config exists
    4. `cat packages/ui/src/lib/utils.ts` -- cn utility exists
  </verify>
  <done>
    @glimmora/ui package is configured with Storybook 10 (ESM-only, a11y addon), Tailwind v4 PostCSS, cn utility (clsx + tailwind-merge), and all dependencies installed (radix-ui, CVA, motion, lucide-react). Storybook preview imports shared theme tokens. Font placeholder structure is ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build all DS-01 through DS-10 components with stories</name>
  <files>
    packages/ui/src/components/typography/typography.tsx
    packages/ui/src/components/typography/typography.stories.tsx
    packages/ui/src/components/typography/index.ts
    packages/ui/src/components/button/button.tsx
    packages/ui/src/components/button/button.stories.tsx
    packages/ui/src/components/button/index.ts
    packages/ui/src/components/input/input.tsx
    packages/ui/src/components/input/input.stories.tsx
    packages/ui/src/components/input/index.ts
    packages/ui/src/components/select/select.tsx
    packages/ui/src/components/select/select.stories.tsx
    packages/ui/src/components/select/index.ts
    packages/ui/src/components/checkbox/checkbox.tsx
    packages/ui/src/components/checkbox/checkbox.stories.tsx
    packages/ui/src/components/checkbox/index.ts
    packages/ui/src/components/radio/radio.tsx
    packages/ui/src/components/radio/radio.stories.tsx
    packages/ui/src/components/radio/index.ts
    packages/ui/src/components/switch/switch.tsx
    packages/ui/src/components/switch/switch.stories.tsx
    packages/ui/src/components/switch/index.ts
    packages/ui/src/components/dialog/dialog.tsx
    packages/ui/src/components/dialog/dialog.stories.tsx
    packages/ui/src/components/dialog/index.ts
    packages/ui/src/components/tooltip/tooltip.tsx
    packages/ui/src/components/tooltip/tooltip.stories.tsx
    packages/ui/src/components/tooltip/index.ts
    packages/ui/src/index.ts
  </files>
  <action>
    Build each component following this exact pattern:
    1. Component file with `"use client"` at top
    2. Use CVA for variant management
    3. Use Radix primitives where applicable (unified `radix-ui` package)
    4. Use `cn()` utility for class merging
    5. Use Tailwind v4 token classes (e.g., `bg-brand-primary`, `text-text-heading`, `rounded-button`)
    6. Create Storybook story file with all variants demonstrated
    7. Create index.ts barrel export

    **DS-02: Typography** `packages/ui/src/components/typography/typography.tsx`:
    ```tsx
    'use client'
    import { cva, type VariantProps } from 'class-variance-authority'
    import { cn } from '../../lib/utils'
    import { forwardRef, type ElementType, type HTMLAttributes } from 'react'

    const headingVariants = cva('font-display text-text-heading', {
      variants: {
        level: {
          h1: 'text-4xl font-medium tracking-tight',
          h2: 'text-3xl font-medium tracking-tight',
          h3: 'text-2xl font-medium',
          h4: 'text-xl font-medium',
        },
      },
      defaultVariants: { level: 'h1' },
    })

    // Heading, Body, Label, Caption components
    ```

    Export 4 components: `Heading`, `Body`, `Label`, `Caption`. Each uses `forwardRef` with appropriate HTML element. Heading uses `font-display`, all others use `font-body`.

    Body variants: `default` (text-base text-text-body), `small` (text-sm), `large` (text-lg).
    Label: `text-sm font-medium text-text-heading`.
    Caption: `text-xs text-text-caption`.

    Story file should show all variants with GlimmoraTeam-relevant sample text.

    **DS-04: Button** `packages/ui/src/components/button/button.tsx`:
    Use CVA with these exact variants:
    - **variant**: `primary` (bg-brand-primary text-white hover:bg-brand-primary/90), `secondary` (bg-border text-text-body hover:bg-hover), `ghost` (bg-transparent text-brand-primary hover:bg-hover), `destructive` (bg-status-urgent text-white hover:bg-status-urgent/90)
    - **size**: `sm` (h-8 px-3 text-sm), `md` (h-10 px-4 text-sm), `lg` (h-12 px-6 text-base)
    - All buttons: `rounded-button` (maps to --radius-button: 10px), `font-body font-medium`, `transition-all duration-150`, `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2`
    - Add `loading` boolean prop that shows a spinner (Loader2 from lucide-react, `animate-spin`) and disables the button
    - Add `icon` slot for leading icon
    - Use `Slot` from radix-ui for `asChild` prop support (allows rendering as `<a>` or other elements)
    - Use `forwardRef`

    Story: Show all 4 variants x 3 sizes, loading state, icon button, disabled state.

    **DS-05: Input** `packages/ui/src/components/input/input.tsx`:
    Three sub-components:
    - `TextInput`: Standard text input styled with warm-earth tokens. `border-border`, `bg-bg-card`, `text-text-body`, `rounded-inner`, `focus:ring-2 focus:ring-brand-primary`. Include `label` and `error` props for integrated form layout.
    - `Textarea`: Multi-line version with `min-h-[80px]`, same styling.
    - `PasswordInput`: TextInput with toggle visibility button (Eye/EyeOff from lucide-react).

    Story: Show TextInput (default, with label, with error, disabled), Textarea, PasswordInput.

    **DS-06: Select** `packages/ui/src/components/select/select.tsx`:
    Wrap Radix Select from `radix-ui` package:
    ```tsx
    import { Select as RadixSelect } from 'radix-ui'
    ```
    Export: `Select` (root), `SelectTrigger`, `SelectContent`, `SelectItem`, `SelectGroup`, `SelectLabel`.
    Style trigger like Input (same border, bg, rounded). Content with `bg-bg-card shadow-card rounded-card`. Items with `hover:bg-hover`.

    Story: Show basic select with 3-5 options, grouped select, disabled select.

    **DS-07: Checkbox** `packages/ui/src/components/checkbox/checkbox.tsx`:
    Wrap Radix Checkbox from `radix-ui`:
    ```tsx
    import { Checkbox as RadixCheckbox } from 'radix-ui'
    ```
    Style: `border-border`, checked state `bg-brand-primary border-brand-primary`, check icon in white (Check from lucide-react). Include integrated `label` prop.
    Size: 18x18px (`h-[18px] w-[18px]`).

    Story: Show unchecked, checked, indeterminate, disabled, with label.

    **DS-07: Radio** `packages/ui/src/components/radio/radio.tsx`:
    Wrap Radix RadioGroup from `radix-ui`:
    ```tsx
    import { RadioGroup } from 'radix-ui'
    ```
    Export: `RadioGroup` (wrapper), `RadioItem` (individual radio).
    Style: circular border, selected state with `bg-brand-primary` inner dot. Include `label` prop on RadioItem.

    Story: Show radio group with 3-4 options, disabled option, horizontal layout variant.

    **DS-08: Switch** `packages/ui/src/components/switch/switch.tsx`:
    Wrap Radix Switch from `radix-ui`:
    ```tsx
    import { Switch as RadixSwitch } from 'radix-ui'
    ```
    Style: off state `bg-border`, on state `bg-brand-primary`. Thumb: `bg-white rounded-full`. Transition on thumb slide.
    Include integrated `label` prop.

    Story: Show off, on, disabled, with label.

    **DS-09: Dialog** `packages/ui/src/components/dialog/dialog.tsx`:
    Wrap Radix Dialog from `radix-ui` with `motion` animation:
    ```tsx
    import { Dialog as RadixDialog } from 'radix-ui'
    import { motion, AnimatePresence } from 'motion/react'
    ```
    Export: `Dialog` (root), `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`, `DialogClose`.

    Overlay: `bg-black/40 backdrop-blur-sm` with motion fade.
    Content: `bg-bg-card rounded-card shadow-card-hover p-6` with motion scale-in (from 0.95 to 1, opacity 0 to 1, duration 150ms).
    Use `AnimatePresence` for exit animation.

    Story: Show basic dialog with title/description/close button, dialog with form content, confirmation dialog (destructive action).

    **DS-10: Tooltip** `packages/ui/src/components/tooltip/tooltip.tsx`:
    Wrap Radix Tooltip from `radix-ui`:
    ```tsx
    import { Tooltip as RadixTooltip } from 'radix-ui'
    ```
    Export: `TooltipProvider` (wrap app), `Tooltip` (root), `TooltipTrigger`, `TooltipContent`.
    Content style: `bg-text-heading text-bg-app text-xs px-3 py-1.5 rounded-badge` with motion fade-in.

    Story: Show tooltip on button hover, tooltip with different placements (top, right, bottom, left).

    **Update barrel export** `packages/ui/src/index.ts`:
    ```typescript
    export { cn } from './lib/utils'

    // DS-02: Typography
    export { Heading, Body, Label, Caption } from './components/typography'

    // DS-04: Button
    export { Button, buttonVariants } from './components/button'

    // DS-05: Input
    export { TextInput, Textarea, PasswordInput } from './components/input'

    // DS-06: Select
    export { Select, SelectTrigger, SelectContent, SelectItem, SelectGroup, SelectLabel } from './components/select'

    // DS-07: Checkbox
    export { Checkbox } from './components/checkbox'

    // DS-07: Radio
    export { RadioGroup, RadioItem } from './components/radio'

    // DS-08: Switch
    export { Switch } from './components/switch'

    // DS-09: Dialog
    export { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogClose } from './components/dialog'

    // DS-10: Tooltip
    export { TooltipProvider, Tooltip, TooltipTrigger, TooltipContent } from './components/tooltip'
    ```

    CRITICAL: The barrel export (index.ts) does NOT get `"use client"`. Each individual component file (button.tsx, dialog.tsx, etc.) has `"use client"` at its own top.

    After creating all components:
    1. Run `pnpm install` (in case any deps changed)
    2. Run `pnpm --filter @glimmora/ui exec tsc --noEmit` to verify types
    3. Run `pnpm --filter @glimmora/ui storybook` to verify Storybook launches
    4. Run `pnpm turbo build` to verify all workspaces still build
  </action>
  <verify>
    1. `pnpm --filter @glimmora/ui exec tsc --noEmit` -- zero type errors
    2. `pnpm --filter @glimmora/ui storybook &` then check `http://localhost:6006` renders stories
    3. Count component folders: `ls packages/ui/src/components/` should show: typography, button, input, select, checkbox, radio, switch, dialog, tooltip (9 folders)
    4. Count story files: `find packages/ui/src -name "*.stories.tsx" | wc -l` should be 9
    5. Verify "use client" in each component: `head -1 packages/ui/src/components/button/button.tsx` shows `'use client'`
    6. Verify barrel export does NOT have "use client": `head -1 packages/ui/src/index.ts` should NOT show 'use client'
    7. `pnpm turbo build` succeeds
  </verify>
  <done>
    @glimmora/ui exports all DS-01 through DS-10 components: Typography (Heading, Body, Label, Caption), Button (4 variants, 3 sizes, loading, icon), TextInput, Textarea, PasswordInput, Select (with Trigger/Content/Item/Group/Label), Checkbox, RadioGroup + RadioItem, Switch, Dialog (with Trigger/Content/Header/Title/Description/Footer/Close), Tooltip (with Provider/Trigger/Content). All components use Radix primitives, CVA for variants, warm-earth Tailwind tokens, and have "use client" directives. Storybook 10 launches with stories for every component. pnpm turbo build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @glimmora/ui exec tsc --noEmit` -- zero type errors
2. `pnpm --filter @glimmora/ui storybook` -- launches at port 6006
3. In Storybook: Button story shows Primary/Secondary/Ghost/Destructive variants with correct colors
4. In Storybook: Dialog story opens an animated modal with brand styling
5. In Storybook: All 9 component groups have stories
6. Accessibility: Run a11y addon checks in Storybook (should be visible per story)
7. `pnpm turbo build` -- all workspaces build with zero errors
</verification>

<success_criteria>
- Storybook 10 launches at port 6006 with all foundational component stories
- Button has Primary (#A0614A), Secondary, Ghost, Destructive variants rendering correctly
- Input components have warm-earth styling (border-border, bg-bg-card, focus ring)
- All Radix components (Select, Checkbox, Radio, Switch, Dialog, Tooltip) are functional and interactive
- Typography uses font-display for Heading and font-body for Body/Label/Caption
- Each component .tsx file has "use client" at top; barrel index.ts does NOT
- a11y addon is configured and available per story
- pnpm turbo build succeeds across all workspaces
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-infrastructure-ds-foundation/01-03-SUMMARY.md`
</output>

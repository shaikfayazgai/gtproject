---
phase: 04-mentor-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/mentor.ts
  - packages/types/src/index.ts
  - packages/ui/package.json
  - packages/ui/src/components/resizable-panels/resizable-panels.tsx
  - packages/ui/src/components/resizable-panels/index.ts
  - packages/ui/src/index.ts
  - apps/mentor-portal/package.json
  - apps/mentor-portal/src/app/layout.tsx
  - apps/mentor-portal/src/app/page.tsx
  - apps/mentor-portal/src/app/(pre-auth)/layout.tsx
  - apps/mentor-portal/src/app/(pre-auth)/page.tsx
  - apps/mentor-portal/src/app/(pre-auth)/apply/page.tsx
  - apps/mentor-portal/src/app/(pre-auth)/onboarding/layout.tsx
  - apps/mentor-portal/src/app/(pre-auth)/onboarding/profile/page.tsx
  - apps/mentor-portal/src/app/(pre-auth)/onboarding/skills/page.tsx
  - apps/mentor-portal/src/app/(pre-auth)/onboarding/capacity/page.tsx
  - apps/mentor-portal/src/app/(pre-auth)/onboarding/orientation/page.tsx
  - apps/mentor-portal/src/components/application/application-form.tsx
  - apps/mentor-portal/src/components/application/application-status.tsx
  - apps/mentor-portal/src/components/application/index.ts
  - apps/mentor-portal/src/components/onboarding/mentor-profile-step.tsx
  - apps/mentor-portal/src/components/onboarding/expertise-skills-step.tsx
  - apps/mentor-portal/src/components/onboarding/capacity-step.tsx
  - apps/mentor-portal/src/components/onboarding/orientation-step.tsx
  - apps/mentor-portal/src/components/onboarding/index.ts
  - apps/mentor-portal/src/store/auth-store.ts
  - apps/mentor-portal/src/lib/msw/factories/common.ts
  - apps/mentor-portal/src/lib/msw/factories/mentor.ts
  - apps/mentor-portal/src/lib/msw/handlers/auth.ts
  - apps/mentor-portal/src/lib/msw/handlers/application.ts
  - apps/mentor-portal/src/lib/msw/handlers/onboarding.ts
  - apps/mentor-portal/src/lib/msw/handlers/index.ts
  - apps/mentor-portal/src/lib/msw/handlers.ts
  - apps/mentor-portal/src/lib/msw/browser.ts
  - pnpm-lock.yaml
autonomous: true

must_haves:
  truths:
    - "Mentor applicant can fill out application form with expertise areas, credentials text, and availability -- and submit it"
    - "After submission, applicant sees APG review status (pending -> under_review -> approved/rejected with reason)"
    - "Approved mentor completes 4-step onboarding: Profile, Expertise+Skill Tags, Capacity hours, Orientation+Code of Conduct"
    - "Onboarding uses Stepper component with 4 labeled steps and progression"
    - "Orientation step requires checkbox acceptance of code of conduct before proceeding"
    - "ResizablePanelGroup, ResizablePanel, and ResizableHandle are exported from @glimmora/ui"
    - "MentorApplication, MentorProfile (extended), ReviewQueueItem, ReviewDecision, SkillTagVerificationRequest types exist in @glimmora/types"
  artifacts:
    - path: "packages/types/src/mentor.ts"
      provides: "All mentor-specific types (MP-01 through MP-20)"
      contains: "MentorApplication"
    - path: "packages/ui/src/components/resizable-panels/resizable-panels.tsx"
      provides: "Thin wrapper over react-resizable-panels v4 for 3-panel review layout"
      contains: "ResizablePanelGroup"
    - path: "apps/mentor-portal/src/app/(pre-auth)/apply/page.tsx"
      provides: "Mentor application page (MP-01)"
    - path: "apps/mentor-portal/src/app/(pre-auth)/onboarding/orientation/page.tsx"
      provides: "Final onboarding step with Code of Conduct (MP-03)"
    - path: "apps/mentor-portal/src/store/auth-store.ts"
      provides: "Zustand auth store for mentor portal"
  key_links:
    - from: "packages/ui/src/components/resizable-panels/resizable-panels.tsx"
      to: "react-resizable-panels"
      via: "Thin wrapper using v4 API (Group/Panel/Separator)"
      pattern: "import.*from.*react-resizable-panels"
    - from: "apps/mentor-portal/src/lib/msw/handlers/application.ts"
      to: "packages/types/src/mentor.ts"
      via: "MentorApplication type shapes mock responses"
      pattern: "MentorApplication"
---

<objective>
Set up the Mentor Portal foundation: new @glimmora/types for all mentor domain types, install react-resizable-panels in @glimmora/ui with wrapper components, add lucide-react to mentor-portal, build the pre-auth application flow (MP-01, MP-02), and the 4-step mentor onboarding (MP-03) with MSW mock handlers.

Purpose: This is the foundation plan for Phase 4. It creates the type contracts that all subsequent plans depend on, installs the critical new dependency (react-resizable-panels) needed for the 3-panel review layout, and delivers the complete pre-auth mentor experience from application to onboarding completion.

Output: @glimmora/types with mentor.ts exports, @glimmora/ui with ResizablePanel wrapper components, Mentor Portal with application form, APG status display, 4-step onboarding, auth store, and MSW handlers for auth/application/onboarding.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mentor-portal/04-RESEARCH.md

Key codebase references -- patterns to follow:
@apps/women-portal/src/app/(pre-auth)/layout.tsx
@apps/women-portal/src/app/(pre-auth)/register/page.tsx
@apps/university-portal/src/store/auth-store.ts
@apps/university-portal/src/lib/msw/handlers/auth.ts
@apps/university-portal/src/lib/msw/handlers/index.ts
@apps/university-portal/src/lib/msw/factories/common.ts
@apps/women-portal/src/app/(pre-auth)/onboarding/layout.tsx

Existing types to extend:
@packages/types/src/user.ts (MentorProfile already exists here -- mentor.ts adds application/review types)
@packages/types/src/index.ts

Component library:
@packages/ui/src/index.ts
@packages/ui/src/components/stepper/stepper.tsx
@packages/ui/package.json

CRITICAL constraints:
- react-resizable-panels v4 API: `import { Group, Panel, Separator } from 'react-resizable-panels'` — uses `orientation` prop (NOT `direction`). Do NOT use v3 names (PanelGroup/PanelResizeHandle)
- @glimmora/ui Label is a <p> element, NOT a <label> -- use native <label> HTML for htmlFor
- lucide-react must be added as direct dependency to mentor-portal package.json
- MSW handlers go in handlers/ directory structure (not flat file)
- Orientation step MUST require code of conduct acceptance (checkbox) before proceeding
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mentor types, install react-resizable-panels, add ResizablePanel wrappers to @glimmora/ui, install lucide-react in mentor-portal</name>
  <files>
    packages/types/src/mentor.ts
    packages/types/src/index.ts
    packages/ui/package.json
    packages/ui/src/components/resizable-panels/resizable-panels.tsx
    packages/ui/src/components/resizable-panels/index.ts
    packages/ui/src/index.ts
    apps/mentor-portal/package.json
    pnpm-lock.yaml
  </files>
  <action>
**Step 1: Create `packages/types/src/mentor.ts` with all mentor domain types.**

```typescript
export type MentorApplicationStatus = 'pending' | 'under_review' | 'approved' | 'rejected'
export type ReviewDecisionType = 'approve' | 'rework_required' | 'reject'
export type SLAStatus = 'normal' | 'warning' | 'urgent' | 'overdue'

export interface MentorApplication {
  id: string
  expertiseAreas: string[]
  credentials: string
  availability: string  // e.g. "10h/week"
  status: MentorApplicationStatus
  submittedAt: string
  reviewedAt?: string
  rejectionReason?: string
}

export interface MentorImpactMetrics {
  totalReviews: number
  averageReviewHours: number
  reworkRate: number       // 0-1 percentage
  appealsOverturnedRate: number  // 0-1 percentage
  pendingReviews: number
}

export interface ReviewQueueItem {
  id: string
  taskId: string
  taskTitle: string
  submittedAt: string
  slaDeadline: string       // ISO string
  status: 'pending' | 'in_progress' | 'completed'
  skillTags: string[]
  submissionCount: number
  decision?: ReviewDecisionType
  appealStatus?: 'none' | 'pending' | 'upheld' | 'overturned'
  hasSLAExtensionPending?: boolean  // true when extension is awaiting admin approval
}

export interface ReviewDetail {
  id: string
  taskId: string
  taskTitle: string
  taskBrief: string
  deliverables: string[]
  skillTagsRequired: string[]
  evidences: ReviewEvidence[]
  submittedAt: string
  slaDeadline: string
  submissionCount: number
}

export interface ReviewEvidence {
  id: string
  type: 'code' | 'document' | 'link' | 'video' | 'text'
  content: string
  submittedAt: string
  // NO contributor identity -- blind review
  language?: string     // for code
  filename?: string     // for code/document
  fileSize?: string     // for document
  fileType?: string     // for document
  downloadUrl?: string  // for document
  url?: string          // for link/video
  title?: string        // for link/video
  description?: string  // for link
}

export interface ReviewDecision {
  reviewId: string
  type: ReviewDecisionType
  feedbackComment?: string
  reworkItems?: string         // required for rework_required
  guidanceNotes?: string       // optional for rework_required
  rejectionReason?: string     // required for reject
  nonComplianceEvidence?: string  // required for reject
}

export interface SkillTagVerificationRequest {
  id: string
  contributorSeed: string    // anonymous identifier (not a name)
  skillTag: string
  evidenceSummary: string
  status: 'pending' | 'verified' | 'disputed'
  submittedAt: string
}

export interface MentorConversation {
  id: string
  subject: string
  lastMessage: string
  lastMessageAt: string
  unread: boolean
}
```

**Step 2: Update `packages/types/src/index.ts` to re-export mentor types.**

Add at the end:
```typescript
export type { MentorApplicationStatus, ReviewDecisionType, SLAStatus, MentorApplication, MentorImpactMetrics, ReviewQueueItem, ReviewDetail, ReviewEvidence, ReviewDecision, SkillTagVerificationRequest, MentorConversation } from './mentor'
```

Note: MentorTier and MentorProfile already exist in user.ts and are already exported.

**Step 3: Install react-resizable-panels in @glimmora/ui.**

```bash
pnpm add react-resizable-panels --filter @glimmora/ui
```

**Step 4: Create ResizablePanel wrapper components.**

Create directory `packages/ui/src/components/resizable-panels/`.

**`packages/ui/src/components/resizable-panels/resizable-panels.tsx`:**
```typescript
'use client'
import { forwardRef } from 'react'
import { Group, Panel, Separator } from 'react-resizable-panels'
import { cn } from '../../lib/utils'

// react-resizable-panels v4 API: Group, Panel, Separator
// v4 uses `orientation` prop (NOT `direction`)

type ResizablePanelGroupProps = React.ComponentProps<typeof Group> & {
  className?: string
}

export const ResizablePanelGroup = forwardRef<
  React.ComponentRef<typeof Group>,
  ResizablePanelGroupProps
>(({ orientation = 'horizontal', className, ...props }, ref) => (
  <Group
    orientation={orientation}
    className={cn('flex h-full w-full', className)}
    {...props}
    ref={ref}
  />
))
ResizablePanelGroup.displayName = 'ResizablePanelGroup'

export const ResizablePanel = Panel

type ResizableHandleProps = React.ComponentProps<typeof Separator> & {
  className?: string
}

export const ResizableHandle = forwardRef<
  React.ComponentRef<typeof Separator>,
  ResizableHandleProps
>(({ className, ...props }, ref) => (
  <Separator
    className={cn(
      'w-1 bg-border hover:bg-brand-primary/30 cursor-col-resize transition-colors data-[resize-handle-active]:bg-brand-primary/50',
      className
    )}
    {...props}
    ref={ref}
  />
))
ResizableHandle.displayName = 'ResizableHandle'
```

**`packages/ui/src/components/resizable-panels/index.ts`:**
```typescript
export { ResizablePanelGroup, ResizablePanel, ResizableHandle } from './resizable-panels'
```

**Step 5: Export from @glimmora/ui index.**

Add to `packages/ui/src/index.ts`:
```typescript
// Resizable Panels (for 3-panel review layout)
export { ResizablePanelGroup, ResizablePanel, ResizableHandle } from './components/resizable-panels'
```

**Step 6: Install lucide-react in mentor-portal.**

```bash
pnpm add lucide-react --filter @glimmora/mentor-portal
```

This is needed for sidebar icons and various UI icons throughout the portal (same decision as women-portal and university-portal).
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/ui` -- builds without errors.
Run `grep "react-resizable-panels" packages/ui/package.json` -- must match.
Run `grep "lucide-react" apps/mentor-portal/package.json` -- must match.
Run `grep "MentorApplication" packages/types/src/mentor.ts` -- must match.
Run `grep "mentor" packages/types/src/index.ts` -- must match mentor exports.
Run `grep "ResizablePanelGroup" packages/ui/src/index.ts` -- must match.
Run `grep "from 'react-resizable-panels'" packages/ui/src/components/resizable-panels/resizable-panels.tsx | grep "Group\|Panel\|Separator"` -- must match (verifies v4 imports).
Run `grep -i "PanelGroup\|PanelResizeHandle\|direction=" packages/ui/src/components/resizable-panels/resizable-panels.tsx` -- must return ZERO matches (verifies v3 API not used).
  </verify>
  <done>@glimmora/types has mentor.ts with all domain types (MentorApplication, ReviewQueueItem, ReviewDetail, ReviewEvidence, ReviewDecision, SkillTagVerificationRequest, MentorConversation, MentorImpactMetrics). @glimmora/ui has ResizablePanelGroup/ResizablePanel/ResizableHandle wrappers exported. lucide-react installed in mentor-portal.</done>
</task>

<task type="auto">
  <name>Task 2: Build mentor application flow, 4-step onboarding, auth store, and MSW handlers</name>
  <files>
    apps/mentor-portal/src/app/layout.tsx
    apps/mentor-portal/src/app/page.tsx
    apps/mentor-portal/src/app/(pre-auth)/layout.tsx
    apps/mentor-portal/src/app/(pre-auth)/page.tsx
    apps/mentor-portal/src/app/(pre-auth)/apply/page.tsx
    apps/mentor-portal/src/app/(pre-auth)/onboarding/layout.tsx
    apps/mentor-portal/src/app/(pre-auth)/onboarding/profile/page.tsx
    apps/mentor-portal/src/app/(pre-auth)/onboarding/skills/page.tsx
    apps/mentor-portal/src/app/(pre-auth)/onboarding/capacity/page.tsx
    apps/mentor-portal/src/app/(pre-auth)/onboarding/orientation/page.tsx
    apps/mentor-portal/src/components/application/application-form.tsx
    apps/mentor-portal/src/components/application/application-status.tsx
    apps/mentor-portal/src/components/application/index.ts
    apps/mentor-portal/src/components/onboarding/mentor-profile-step.tsx
    apps/mentor-portal/src/components/onboarding/expertise-skills-step.tsx
    apps/mentor-portal/src/components/onboarding/capacity-step.tsx
    apps/mentor-portal/src/components/onboarding/orientation-step.tsx
    apps/mentor-portal/src/components/onboarding/index.ts
    apps/mentor-portal/src/store/auth-store.ts
    apps/mentor-portal/src/lib/msw/factories/common.ts
    apps/mentor-portal/src/lib/msw/factories/mentor.ts
    apps/mentor-portal/src/lib/msw/handlers/auth.ts
    apps/mentor-portal/src/lib/msw/handlers/application.ts
    apps/mentor-portal/src/lib/msw/handlers/onboarding.ts
    apps/mentor-portal/src/lib/msw/handlers/index.ts
    apps/mentor-portal/src/lib/msw/handlers.ts
    apps/mentor-portal/src/lib/msw/browser.ts
  </files>
  <action>
**Step 1: Remove canary page and set up route groups.**

Delete `apps/mentor-portal/src/app/canary/` directory and the existing canary content.

Update `apps/mentor-portal/src/app/layout.tsx` to include Providers wrapper (same pattern as university-portal). The root layout wraps children in Providers (MSWProvider + QueryProvider). Keep `globals.css` import.

Update `apps/mentor-portal/src/app/page.tsx` to redirect to `/(pre-auth)` landing.

**Step 2: Create pre-auth layout.**

**`apps/mentor-portal/src/app/(pre-auth)/layout.tsx`:**
Same pattern as women-portal pre-auth layout -- centered container with max-w-md, bg-bg-app.

**`apps/mentor-portal/src/app/(pre-auth)/page.tsx`:**
Landing page with:
- Glimmora branding
- Login form (email + password) calling POST /api/auth/login
- "Apply to become a Mentor" link to /apply
- "Check application status" link

On successful login, check user status from auth store:
- If `applicationStatus === 'approved' && !onboardingCompleted` -> redirect to /onboarding/profile
- If `onboardingCompleted` -> redirect to /queue (authenticated area)
- Otherwise show login

**Step 3: Create application form and status pages.**

**`apps/mentor-portal/src/components/application/application-form.tsx`:**
```
'use client'
```
Form fields:
- Expertise Areas: multi-select or tag input. Use a set of checkboxes for common areas (Frontend, Backend, Data Science, UI/UX, DevOps, QA, Technical Writing, Mobile) plus a text input for "Other".
- Credentials: Textarea -- "Describe your professional experience and qualifications"
- Availability: Select dropdown with options: "5h/week", "10h/week", "15h/week", "20h/week", "20h+/week"
- Submit button calls POST /api/mentor/application

On submission success, redirect to the status page showing "Application submitted" with APG review status.

**`apps/mentor-portal/src/components/application/application-status.tsx`:**
Displays the current application status:
- `pending`: "Your application has been submitted. APG is reviewing your credentials."
- `under_review`: "Your application is under active review by the APG."
- `approved`: "Congratulations! Your application has been approved." + "Begin Onboarding" button -> /onboarding/profile
- `rejected`: "Your application was not approved." + rejection reason display + "Apply Again" button

Uses TanStack Query to poll GET /api/mentor/application every 5 seconds (for demo, the mock handler will auto-transition from pending -> approved after the first poll).

**Step 4: Create 4-step onboarding.**

**`apps/mentor-portal/src/app/(pre-auth)/onboarding/layout.tsx`:**
Onboarding layout with Stepper component (from @glimmora/ui) showing 4 steps:
1. "Mentor Profile"
2. "Expertise & Skills"
3. "Capacity"
4. "Orientation"

Derive current step from pathname.

**`apps/mentor-portal/src/components/onboarding/mentor-profile-step.tsx`:**
Fields: Display Name, Bio (Textarea), Profile photo URL (text input -- mock). On submit POST /api/mentor/onboarding/profile, navigate to /onboarding/skills.

**`apps/mentor-portal/src/components/onboarding/expertise-skills-step.tsx`:**
Fields: Expertise areas (same multi-select as application, pre-filled from application data), Specific skill tags (tag input -- text + Enter to add, shows Tag components, deletable). On submit POST /api/mentor/onboarding/skills, navigate to /onboarding/capacity.

**`apps/mentor-portal/src/components/onboarding/capacity-step.tsx`:**
Fields: Weekly review hours (Slider from @glimmora/ui, range 5-40, step 5, default 10), preferred review times (checkboxes: Morning, Afternoon, Evening, Weekend). On submit POST /api/mentor/onboarding/capacity, navigate to /onboarding/orientation.

**`apps/mentor-portal/src/components/onboarding/orientation-step.tsx`:**
Content sections:
1. "Your Role as a Mentor" -- paragraph explaining blind review, structured feedback, SLA expectations
2. "Code of Conduct" -- list of 5-6 principles (Fair assessment, Constructive feedback, Timely reviews, Confidentiality, No direct contributor contact)
3. Checkbox: "I have read and accept the Mentor Code of Conduct" -- MUST be checked before "Complete Onboarding" button is enabled
4. "Complete Onboarding" button calls POST /api/mentor/onboarding/orientation, then sets onboardingCompleted in auth store and redirects to /queue

CRITICAL: The checkbox acceptance is a REQUIRED gate. Button must be disabled until checked.

**Step 5: Create auth store.**

**`apps/mentor-portal/src/store/auth-store.ts`:**
```typescript
import { create } from 'zustand'
import { persist, createJSONStorage } from 'zustand/middleware'
import type { MentorProfile } from '@glimmora/types'

interface AuthState {
  user: MentorProfile | null
  isAuthenticated: boolean
  applicationStatus: 'none' | 'pending' | 'under_review' | 'approved' | 'rejected'
  onboardingStep: number
  onboardingCompleted: boolean
  setUser: (user: MentorProfile) => void
  setApplicationStatus: (status: AuthState['applicationStatus']) => void
  setOnboardingStep: (step: number) => void
  completeOnboarding: () => void
  logout: () => void
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      isAuthenticated: false,
      applicationStatus: 'none',
      onboardingStep: 0,
      onboardingCompleted: false,
      setUser: (user) => set({ user, isAuthenticated: true }),
      setApplicationStatus: (status) => set({ applicationStatus: status }),
      setOnboardingStep: (step) => set({ onboardingStep: step }),
      completeOnboarding: () => set({ onboardingCompleted: true, onboardingStep: 4 }),
      logout: () => set({ user: null, isAuthenticated: false, applicationStatus: 'none', onboardingStep: 0, onboardingCompleted: false }),
    }),
    { name: 'glimmora-mentor-auth', storage: createJSONStorage(() => localStorage) }
  )
)
```

**Step 6: Create MSW factories and handlers.**

**`apps/mentor-portal/src/lib/msw/factories/common.ts`:**
Copy randomId and isoNow from women-portal/src/lib/msw/factories/common.ts.

**`apps/mentor-portal/src/lib/msw/factories/mentor.ts`:**
```typescript
import { randomId, isoNow } from './common'
import type { MentorProfile, MentorTier } from '@glimmora/types'

export function createMockMentorProfile(overrides?: Partial<MentorProfile>): MentorProfile {
  return {
    id: randomId('user'),
    role: 'mentor-reviewer',
    displayName: 'Dr. Mentor',
    email: 'mentor@glimmora.dev',
    isActive: true,
    tier: 'silver' as MentorTier,
    expertiseAreas: ['Frontend', 'Backend', 'UI/UX'],
    capacityHoursPerWeek: 15,
    totalReviews: 47,
    createdAt: '2025-11-15T00:00:00Z',
    updatedAt: isoNow(),
    ...overrides,
  }
}
```

**MSW handlers:**

**`handlers/auth.ts`:**
- POST /api/auth/login -> returns mock token + sets user as approved mentor (for quick login flow)
- GET /api/auth/me -> returns MentorProfile or applicant status based on auth store state

**`handlers/application.ts`:**
- POST /api/mentor/application -> 201, returns MentorApplication with status 'pending'
- GET /api/mentor/application -> returns application. First call returns 'pending', subsequent calls return 'approved' (simulates APG review completing)

**`handlers/onboarding.ts`:**
- GET /api/mentor/onboarding/status -> { completedSteps: string[] }
- POST /api/mentor/onboarding/profile -> 200
- POST /api/mentor/onboarding/skills -> 200
- POST /api/mentor/onboarding/capacity -> 200
- POST /api/mentor/onboarding/orientation -> 200

**`handlers/index.ts`:**
Aggregates and re-exports all handlers from auth, application, onboarding.

**`handlers.ts`** (root msw dir): Re-exports from handlers/index.ts for backward compatibility with existing browser.ts.

Update **`browser.ts`** if needed to import from the new handlers path.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/mentor-portal` -- builds without errors.
Verify pre-auth pages exist: `ls apps/mentor-portal/src/app/\(pre-auth\)/{page.tsx,apply/page.tsx}` -- both exist.
Verify onboarding pages: `ls apps/mentor-portal/src/app/\(pre-auth\)/onboarding/{profile,skills,capacity,orientation}/page.tsx` -- all 4 exist.
Verify auth store: `grep "onboardingCompleted" apps/mentor-portal/src/store/auth-store.ts` -- must match.
Verify MSW handler structure: `ls apps/mentor-portal/src/lib/msw/handlers/{auth,application,onboarding,index}.ts` -- all exist.
Verify code of conduct gate: `grep -i "code.*conduct\|accept.*checkbox\|disabled.*!.*accept\|disabled.*!.*agreed" apps/mentor-portal/src/components/onboarding/orientation-step.tsx` -- must match acceptance gate pattern.
  </verify>
  <done>Mentor Portal pre-auth flow complete: landing with login/apply, application form (expertise, credentials, availability), APG status polling, 4-step onboarding (profile, expertise+skill tags, capacity with slider, orientation with code of conduct acceptance gate). Auth store with localStorage persistence. MSW handlers for auth, application, and onboarding endpoints.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/ui --filter=@glimmora/mentor-portal` builds without errors
2. `packages/types/src/mentor.ts` exports MentorApplication, ReviewQueueItem, ReviewDetail, ReviewEvidence, ReviewDecision, SkillTagVerificationRequest, MentorConversation, MentorImpactMetrics
3. `packages/ui/src/index.ts` exports ResizablePanelGroup, ResizablePanel, ResizableHandle
4. `react-resizable-panels` in packages/ui/package.json dependencies
5. `lucide-react` in apps/mentor-portal/package.json dependencies
6. Application form includes expertise areas, credentials textarea, availability select
7. Application status shows pending/under_review/approved/rejected states
8. 4-step onboarding with Stepper component (Profile, Expertise, Capacity, Orientation)
9. Orientation step has Code of Conduct acceptance checkbox that gates the "Complete Onboarding" button
10. Auth store persists to localStorage with application status and onboarding state
11. MSW handlers in handlers/ directory (not flat file): auth.ts, application.ts, onboarding.ts, index.ts
</verification>

<success_criteria>
- Mentor types are available in @glimmora/types for all subsequent plans to import
- ResizablePanel components are available in @glimmora/ui for Plan 04-03 to use
- Mentor applicant can submit application, see APG status transition, and begin onboarding when approved
- 4-step onboarding completes with Code of Conduct acceptance, then auth store marks onboardingCompleted
- All pages build without errors and follow established pre-auth patterns from prior portals
</success_criteria>

<output>
After completion, create `.planning/phases/04-mentor-portal/04-01-SUMMARY.md`
</output>

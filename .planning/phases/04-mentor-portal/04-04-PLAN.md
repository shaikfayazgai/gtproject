---
phase: 04-mentor-portal
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/mentor-portal/src/app/(app)/profile/page.tsx
  - apps/mentor-portal/src/app/(app)/skill-verification/page.tsx
  - apps/mentor-portal/src/app/(app)/messages/page.tsx
  - apps/mentor-portal/src/app/(app)/settings/page.tsx
  - apps/mentor-portal/src/app/(app)/settings/capacity/page.tsx
  - apps/mentor-portal/src/app/(app)/settings/skills/page.tsx
  - apps/mentor-portal/src/app/(app)/settings/notifications/page.tsx
  - apps/mentor-portal/src/components/profile/mentor-profile-page.tsx
  - apps/mentor-portal/src/components/profile/tier-progress-card.tsx
  - apps/mentor-portal/src/components/profile/impact-metrics-card.tsx
  - apps/mentor-portal/src/components/profile/index.ts
  - apps/mentor-portal/src/components/skill-verification/verification-queue.tsx
  - apps/mentor-portal/src/components/skill-verification/verification-item-card.tsx
  - apps/mentor-portal/src/components/skill-verification/dispute-dialog.tsx
  - apps/mentor-portal/src/components/skill-verification/index.ts
  - apps/mentor-portal/src/components/messages/mentor-messages-page.tsx
  - apps/mentor-portal/src/components/messages/index.ts
  - apps/mentor-portal/src/components/settings/capacity-settings-form.tsx
  - apps/mentor-portal/src/components/settings/skills-settings-form.tsx
  - apps/mentor-portal/src/components/settings/notification-prefs-form.tsx
  - apps/mentor-portal/src/components/settings/index.ts
  - apps/mentor-portal/src/lib/msw/handlers/profile.ts
  - apps/mentor-portal/src/lib/msw/handlers/skill-verification.ts
  - apps/mentor-portal/src/lib/msw/handlers/conversations.ts
  - apps/mentor-portal/src/lib/msw/handlers/notifications.ts
  - apps/mentor-portal/src/lib/msw/handlers/index.ts
  - apps/mentor-portal/src/lib/msw/factories/mentor.ts
autonomous: true

must_haves:
  truths:
    - "Tier progress card shows requirements for the NEXT tier with per-metric progress bars and current vs. target values (e.g., 'Reviews: 47/100')"
    - "Impact metrics are displayed: total reviews, average review time, rework rate, appeals overturned rate"
    - "Mentor can see their expertise skill tags with verification status on the profile page"
    - "Skill tag verification queue shows pending contributor skill claims with evidence summary"
    - "Mentor can verify or dispute a skill tag claim -- dispute requires a reason"
    - "Messages page shows async conversations (reply-only -- mentors cannot initiate contact with contributors)"
    - "Capacity settings allow adjusting weekly review hours and toggling temporary pause"
    - "Skills settings allow managing expertise areas and skill tags"
    - "Notification preferences allow toggling in-app and email channels per category"
  artifacts:
    - path: "apps/mentor-portal/src/app/(app)/profile/page.tsx"
      provides: "Mentor profile with tier and metrics (MP-12, MP-13, MP-14)"
    - path: "apps/mentor-portal/src/app/(app)/skill-verification/page.tsx"
      provides: "Skill tag verification queue (MP-15, MP-16)"
    - path: "apps/mentor-portal/src/app/(app)/messages/page.tsx"
      provides: "Async messaging -- reply only (MP-17)"
    - path: "apps/mentor-portal/src/app/(app)/settings/capacity/page.tsx"
      provides: "Capacity settings (MP-18)"
    - path: "apps/mentor-portal/src/app/(app)/settings/skills/page.tsx"
      provides: "Expertise/skill tag management (MP-19)"
    - path: "apps/mentor-portal/src/app/(app)/settings/notifications/page.tsx"
      provides: "Notification preferences (MP-20)"
  key_links:
    - from: "apps/mentor-portal/src/components/profile/mentor-profile-page.tsx"
      to: "/api/mentor/profile"
      via: "TanStack Query fetching MentorProfile + MentorImpactMetrics"
      pattern: "queryKey.*profile"
    - from: "apps/mentor-portal/src/components/skill-verification/verification-queue.tsx"
      to: "/api/mentor/skill-verification"
      via: "TanStack Query fetching SkillTagVerificationRequest[]"
      pattern: "queryKey.*skill-verification"
    - from: "apps/mentor-portal/src/components/messages/mentor-messages-page.tsx"
      to: "/api/mentor/conversations"
      via: "Async messaging -- reply only, no initiation"
      pattern: "queryKey.*conversations"
---

<objective>
Build the remaining Mentor Portal pages: profile (tier status, impact metrics, expertise tags), skill tag verification queue (verify/dispute actions), async messaging (reply-only), and settings (capacity, skills, notifications) -- fulfilling MP-12 through MP-20.

Purpose: These pages complete the mentor experience beyond the core review workflow. Profile gives mentors visibility into their progression. Skill verification is the second key mentor workflow (beyond evidence review). Settings let mentors manage their availability and preferences.

Output: Profile page with tier and metrics, skill verification queue with verify/dispute actions, messages page (reply-only), 3 settings sub-pages (capacity, skills, notifications), and all MSW handlers.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mentor-portal/04-RESEARCH.md
@.planning/phases/04-mentor-portal/04-01-SUMMARY.md
@.planning/phases/04-mentor-portal/04-02-SUMMARY.md

Key codebase references -- patterns to follow:
@apps/women-portal/src/components/messages/messages-page.tsx (async messaging pattern)
@apps/women-portal/src/components/settings/notification-prefs-form.tsx (notification prefs pattern)
@apps/women-portal/src/components/settings/privacy-settings-form.tsx (settings sub-page pattern)
@apps/women-portal/src/app/(app)/settings/page.tsx (settings sidebar nav pattern)

Types:
@packages/types/src/user.ts (MentorProfile, MentorTier)
@packages/types/src/mentor.ts (MentorImpactMetrics, SkillTagVerificationRequest, MentorConversation)
@packages/types/src/notification.ts (NotificationPreference, NotificationChannel, NotificationCategory)
@packages/types/src/message.ts (Message, MessageSenderRole)

Component library:
@packages/ui/src/components/gradient-card/gradient-card.tsx
@packages/ui/src/components/badge/badge.tsx
@packages/ui/src/components/tag/tag.tsx
@packages/ui/src/components/slider/slider.tsx
@packages/ui/src/components/switch/switch.tsx
@packages/ui/src/components/dialog/dialog.tsx

CRITICAL constraints:
- Messages use senderRole display ('You'/'Platform') not names -- privacy-preserving
- Mentors CANNOT initiate conversations -- reply only
- @glimmora/ui Label is a <p> element NOT a <label> -- use native <label> for forms
- Notification prefs use actual @glimmora/types channels (in_app/email) and categories (task_updates/payments/messages/platform)
- Settings root page uses sidebar navigation linking to sub-routes
- GradientCard for metric display cards (not KPIStatCard); 3rd card uses inline style gradient
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build mentor profile page with tier status, impact metrics, and expertise tags</name>
  <files>
    apps/mentor-portal/src/app/(app)/profile/page.tsx
    apps/mentor-portal/src/components/profile/mentor-profile-page.tsx
    apps/mentor-portal/src/components/profile/tier-progress-card.tsx
    apps/mentor-portal/src/components/profile/impact-metrics-card.tsx
    apps/mentor-portal/src/components/profile/index.ts
    apps/mentor-portal/src/app/(app)/skill-verification/page.tsx
    apps/mentor-portal/src/components/skill-verification/verification-queue.tsx
    apps/mentor-portal/src/components/skill-verification/verification-item-card.tsx
    apps/mentor-portal/src/components/skill-verification/index.ts
    apps/mentor-portal/src/lib/msw/handlers/profile.ts
    apps/mentor-portal/src/lib/msw/handlers/skill-verification.ts
    apps/mentor-portal/src/lib/msw/factories/mentor.ts
    apps/mentor-portal/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create the Mentor Profile page.**

**`apps/mentor-portal/src/app/(app)/profile/page.tsx`:**
Server component that renders MentorProfilePage.

**`apps/mentor-portal/src/components/profile/mentor-profile-page.tsx`:**
```typescript
'use client'
```
Fetches GET /api/mentor/profile (returns MentorProfile + MentorImpactMetrics combined).

Page layout:
1. **Header section:** Display name, bio, tier badge, "Member since" date
2. **3 GradientCard summary metrics:**
   - Total Reviews (number) -- primary gradient
   - Avg Review Time (e.g. "4.2h") -- nature gradient
   - Rework Rate (e.g. "12%") -- inline style gradient `background: linear-gradient(135deg, #C4A23A, #3A8FA0)`

   IMPORTANT: Use GradientCard for all 3 (not KPIStatCard).
3. **Tier Progress Card** (TierProgressCard component)
4. **Impact Metrics Card** (ImpactMetricsCard component)
5. **Expertise Tags section:** list of skill tags as Tag components with verification status badges

**Step 2: Create TierProgressCard.**

**`apps/mentor-portal/src/components/profile/tier-progress-card.tsx`:**
Shows current tier with visual indicator and progression requirements:

```
Current Tier: [Silver] (badge with tier color)

Progression to Gold:
- Total reviews: 47/100 [progress bar]
- Average review time: 4.2h (target: <6h) [check mark]
- Rework rate: 12% (target: <15%) [check mark]
- Appeals overturned: 3% (target: <5%) [check mark]
```

Tier colors:
- Bronze: #CD7F32
- Silver: #C0C0C0
- Gold: #FFD700
- Elite: #A0614A (brand terracotta)

Use Progress component from @glimmora/ui for the progress bars.

**Step 3: Create ImpactMetricsCard.**

**`apps/mentor-portal/src/components/profile/impact-metrics-card.tsx`:**
Card displaying all impact metrics in a grid:
- Total Reviews: number
- Average Review Time: formatted as hours
- Rework Rate: percentage with color coding (green <15%, amber 15-25%, red >25%)
- Appeals Overturned Rate: percentage with color coding
- Pending Reviews: number

Uses Card from @glimmora/ui.

**Step 4: Create Skill Tag Verification Queue.**

**`apps/mentor-portal/src/app/(app)/skill-verification/page.tsx`:**
Server component rendering VerificationQueue.

**`apps/mentor-portal/src/components/skill-verification/verification-queue.tsx`:**
Fetches GET /api/mentor/skill-verification -> SkillTagVerificationRequest[].

Page layout:
- Header: "Skill Tag Verification"
- Description: "Verify or dispute contributor skill claims based on their evidence"
- List of VerificationItemCard components
- Empty state if no pending verifications

**`apps/mentor-portal/src/components/skill-verification/verification-item-card.tsx`:**
Each card shows:
- Skill tag name (Tag component)
- Contributor anonymous seed identifier (e.g., "Contributor #alpha-2f3a")
- Evidence summary text
- Submitted date
- Status badge (pending/verified/disputed)
- For pending items: "Verify" button (green) and "Dispute" button (red)
  - "Dispute" opens DisputeDialog (see below)

Verify action: POST /api/mentor/skill-verification/:id/verify -- invalidates skill-verification query on success.

**`apps/mentor-portal/src/components/skill-verification/dispute-dialog.tsx`:**
Extracted as a separate component (parallel to skip-dialog.tsx in 04-02). Uses @glimmora/ui Dialog. Content:
- Title: "Dispute Skill Claim"
- Body: "Provide a reason for disputing this skill tag verification."
- Textarea for reason (required -- submit button disabled if empty)
- Cancel and "Submit Dispute" buttons
- On submit: POST /api/mentor/skill-verification/:id/dispute with { reason }
- On success: invalidate skill-verification query, close dialog

Props: `{ open: boolean; onOpenChange: (open: boolean) => void; verificationId: string }`

The verification-item-card imports and renders DisputeDialog, controlling its `open` state locally.

**Step 5: Create MSW handlers for profile and skill verification.**

**`apps/mentor-portal/src/lib/msw/handlers/profile.ts`:**
- GET /api/mentor/profile -> returns MentorProfile merged with MentorImpactMetrics:
```typescript
{
  ...createMockMentorProfile(),
  totalReviews: 47,
  averageReviewHours: 4.2,
  reworkRate: 0.12,
  appealsOverturnedRate: 0.03,
  pendingReviews: 5,
}
```
- PUT /api/mentor/profile -> 200
- PUT /api/mentor/capacity -> 200
- PUT /api/mentor/skills -> 200

**`apps/mentor-portal/src/lib/msw/handlers/skill-verification.ts`:**
- GET /api/mentor/skill-verification -> returns array of 4-5 SkillTagVerificationRequest items:
```typescript
[
  { id: 'sv-001', contributorSeed: 'alpha-2f3a', skillTag: 'React', evidenceSummary: 'Completed 3 frontend tasks with React components, including a complex data visualization dashboard.', status: 'pending', submittedAt: '2026-02-24T10:00:00Z' },
  { id: 'sv-002', contributorSeed: 'beta-7c1d', skillTag: 'PostgreSQL', evidenceSummary: 'Designed database schema for user management module with migrations and seed data.', status: 'pending', submittedAt: '2026-02-23T14:00:00Z' },
  { id: 'sv-003', contributorSeed: 'gamma-4e9b', skillTag: 'Technical Writing', evidenceSummary: 'Wrote API documentation for 12 endpoints with examples and error handling guides.', status: 'verified', submittedAt: '2026-02-20T09:00:00Z' },
  { id: 'sv-004', contributorSeed: 'delta-8a5f', skillTag: 'DevOps', evidenceSummary: 'Set up CI/CD pipeline with GitHub Actions, Docker containers, and automated testing.', status: 'disputed', submittedAt: '2026-02-19T11:00:00Z' },
]
```
- POST /api/mentor/skill-verification/:id/verify -> 200
- POST /api/mentor/skill-verification/:id/dispute -> 200 (body: { reason })

Update `apps/mentor-portal/src/lib/msw/factories/mentor.ts` if needed to add factory functions for verification data.

**Update handlers/index.ts** to include profile and skill-verification handlers.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/mentor-portal` -- builds without errors.
Verify profile page: `ls apps/mentor-portal/src/app/\(app\)/profile/page.tsx` -- exists.
Verify skill verification page: `ls apps/mentor-portal/src/app/\(app\)/skill-verification/page.tsx` -- exists.
Verify GradientCard usage on profile: `grep "GradientCard" apps/mentor-portal/src/components/profile/mentor-profile-page.tsx` -- must match.
Verify tier progress: `grep -i "bronze\|silver\|gold\|elite" apps/mentor-portal/src/components/profile/tier-progress-card.tsx` -- must match tier names.
Verify dispute dialog is separate file: `ls apps/mentor-portal/src/components/skill-verification/dispute-dialog.tsx` -- must exist.
Verify dispute requires reason: `grep -i "reason\|disabled" apps/mentor-portal/src/components/skill-verification/dispute-dialog.tsx` -- must match required reason pattern.
Verify anonymous identifiers: `grep "contributorSeed\|seed" apps/mentor-portal/src/components/skill-verification/verification-item-card.tsx` -- must match (NOT names/emails).
  </verify>
  <done>Mentor profile page with tier badge, tier progress card (showing requirements for next tier), impact metrics card (5 metrics), expertise skill tags with verification status. Skill verification queue showing contributor skill claims (anonymous identifiers only), with verify button and dispute dialog (requiring reason). MSW handlers for profile and skill verification endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Build messaging, settings pages (capacity, skills, notifications), and finalize MSW handlers</name>
  <files>
    apps/mentor-portal/src/app/(app)/messages/page.tsx
    apps/mentor-portal/src/app/(app)/settings/page.tsx
    apps/mentor-portal/src/app/(app)/settings/capacity/page.tsx
    apps/mentor-portal/src/app/(app)/settings/skills/page.tsx
    apps/mentor-portal/src/app/(app)/settings/notifications/page.tsx
    apps/mentor-portal/src/components/messages/mentor-messages-page.tsx
    apps/mentor-portal/src/components/messages/index.ts
    apps/mentor-portal/src/components/settings/capacity-settings-form.tsx
    apps/mentor-portal/src/components/settings/skills-settings-form.tsx
    apps/mentor-portal/src/components/settings/notification-prefs-form.tsx
    apps/mentor-portal/src/components/settings/index.ts
    apps/mentor-portal/src/lib/msw/handlers/conversations.ts
    apps/mentor-portal/src/lib/msw/handlers/notifications.ts
    apps/mentor-portal/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create Messages page.**

**`apps/mentor-portal/src/app/(app)/messages/page.tsx`:**
Server component rendering MentorMessagesPage.

**`apps/mentor-portal/src/components/messages/mentor-messages-page.tsx`:**
Follow the Women's Portal messages pattern (apps/women-portal/src/components/messages/messages-page.tsx) with these CRITICAL differences:

- Mentors can only REPLY to conversations -- there is NO "New Conversation" button
- Display senderRole as 'You' or 'Platform' (not names -- privacy preserving)
- No ability to contact contributors directly

Layout:
- Left sidebar: conversation list from GET /api/mentor/conversations
  - Each conversation shows: subject, last message preview, time, unread indicator
- Right area: selected conversation messages from GET /api/mentor/conversations/:id/messages
  - Messages show senderRole label, content, timestamp
  - Reply textarea at bottom with Send button
  - POST /api/mentor/conversations/:id/messages to send

If no conversation selected, show empty state: "Select a conversation to view messages."

**Step 2: Create Settings root page.**

**`apps/mentor-portal/src/app/(app)/settings/page.tsx`:**
Follow Women's Portal settings pattern -- sidebar navigation linking to sub-routes:

```typescript
'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { Clock, Brain, Bell } from 'lucide-react'
import { cn } from '@glimmora/ui'
```

Settings nav items:
- Capacity (/settings/capacity) -- Clock icon -- "Manage your weekly review hours and availability"
- Expertise & Skills (/settings/skills) -- Brain icon -- "Update your expertise areas and skill tags"
- Notifications (/settings/notifications) -- Bell icon -- "Configure how you receive notifications"

Each item is a card-style link with icon, title, and description. Active state highlighted.

When no sub-route is selected (at /settings root), show this settings hub page with the 3 cards as navigation.

**Step 3: Create Capacity Settings page.**

**`apps/mentor-portal/src/app/(app)/settings/capacity/page.tsx`:**
Renders CapacitySettingsForm.

**`apps/mentor-portal/src/components/settings/capacity-settings-form.tsx`:**
```typescript
'use client'
```

Form fields:
- Weekly Review Hours: Slider (5-40, step 5) with current value display
  - Uses @glimmora/ui Slider component
- Temporary Pause: Switch toggle
  - "Pause my review queue temporarily" -- when enabled, no new reviews are assigned
  - Uses @glimmora/ui Switch component
- Current capacity display: "You are available for {X} hours of review per week"

Save button calls PUT /api/mentor/capacity with { hoursPerWeek, isPaused }.

Use native `<label>` elements for form field labels (NOT @glimmora/ui Label).

**Step 4: Create Skills Settings page.**

**`apps/mentor-portal/src/app/(app)/settings/skills/page.tsx`:**
Renders SkillsSettingsForm.

**`apps/mentor-portal/src/components/settings/skills-settings-form.tsx`:**
```typescript
'use client'
```

Two sections:

1. **Expertise Areas** -- same checkboxes as application form (Frontend, Backend, Data Science, UI/UX, DevOps, QA, Technical Writing, Mobile) with current state pre-filled from GET /api/mentor/profile
2. **Skill Tags** -- tag input (text input + Enter to add, Tag components with X to remove)

Save button calls PUT /api/mentor/skills with { expertiseAreas, skillTags }.

**Step 5: Create Notification Preferences page.**

**`apps/mentor-portal/src/app/(app)/settings/notifications/page.tsx`:**
Renders NotificationPrefsForm.

**`apps/mentor-portal/src/components/settings/notification-prefs-form.tsx`:**
Follow Women's Portal notification-prefs-form.tsx pattern:

Grid with rows for each NotificationCategory and columns for each NotificationChannel:

Categories for mentor context:
- Review Assignments ("New reviews assigned to your queue")
- SLA Reminders ("Reminders about approaching SLA deadlines")
- Decision Outcomes ("Updates on appeal outcomes for your decisions")
- Platform Updates ("Platform announcements and feature updates")

Channels: In-App, Email (2 channels for mentor -- no WhatsApp)

Each cell is a Switch component. Fetches GET /api/mentor/notification-preferences, saves via PUT.

Use native `<label>` elements, NOT @glimmora/ui Label.

**Step 6: Create MSW handlers for conversations and notifications.**

**`apps/mentor-portal/src/lib/msw/handlers/conversations.ts`:**
- GET /api/mentor/conversations -> returns 3 mock conversations:
```typescript
[
  { id: 'conv-001', subject: 'Review Calibration Session', lastMessage: 'The next calibration session is scheduled for March 5th.', lastMessageAt: '2026-02-25T16:00:00Z', unread: true },
  { id: 'conv-002', subject: 'SLA Extension Approved', lastMessage: 'Your SLA extension for review #RV-042 has been approved.', lastMessageAt: '2026-02-24T11:00:00Z', unread: false },
  { id: 'conv-003', subject: 'Welcome to the Mentor Program', lastMessage: 'Thank you for completing your orientation. Here are some tips...', lastMessageAt: '2026-02-20T09:00:00Z', unread: false },
]
```
- GET /api/mentor/conversations/:id/messages -> returns 3-5 messages per conversation with senderRole: 'platform' or 'mentor'
- POST /api/mentor/conversations/:id/messages -> 201

**`apps/mentor-portal/src/lib/msw/handlers/notifications.ts`:**
- GET /api/mentor/notification-preferences -> returns NotificationPreference[] for 4 categories x 2 channels
- PUT /api/mentor/notification-preferences -> 200

**Update handlers/index.ts** to include conversations and notifications handlers.

**Step 7: Create barrel exports for messages and settings components.**
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/mentor-portal` -- builds without errors.
Verify all pages exist:
- `ls apps/mentor-portal/src/app/\(app\)/messages/page.tsx` -- exists
- `ls apps/mentor-portal/src/app/\(app\)/settings/{page.tsx,capacity/page.tsx,skills/page.tsx,notifications/page.tsx}` -- all 4 exist
Verify reply-only messaging: `grep -i "new.*conversation\|initiate\|compose" apps/mentor-portal/src/components/messages/mentor-messages-page.tsx` -- ZERO matches (mentors cannot initiate).
Verify no compose affordance: `grep -i "new.*message\|new.*thread\|start.*conversation\|new.*conversation" apps/mentor-portal/src/components/messages/mentor-messages-page.tsx` -- must return ZERO matches.
Verify capacity slider: `grep "Slider" apps/mentor-portal/src/components/settings/capacity-settings-form.tsx` -- must match.
Verify pause toggle: `grep "Switch\|isPaused\|pause" apps/mentor-portal/src/components/settings/capacity-settings-form.tsx` -- must match.
Verify notification prefs: `grep "Switch" apps/mentor-portal/src/components/settings/notification-prefs-form.tsx` -- must match.
Verify native labels: `grep "<label" apps/mentor-portal/src/components/settings/capacity-settings-form.tsx` -- must match (NOT @glimmora/ui Label).
Verify MSW handler count: `grep "http\.\(get\|post\|put\)" apps/mentor-portal/src/lib/msw/handlers/*.ts | wc -l` -- should be at least 15 endpoints total.
  </verify>
  <done>Messages page with reply-only conversations (no initiate button, senderRole display). Settings hub with 3 sub-pages: capacity (slider + pause toggle), skills (expertise checkboxes + skill tag input), notifications (4 categories x 2 channels grid with switches). MSW handlers for conversations and notification preferences. All portal sidebar pages functional.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/mentor-portal` builds without errors
2. Profile page shows tier badge (Bronze/Silver/Gold/Elite) with visual indicator
3. Tier progress card shows progression requirements with progress bars
4. Impact metrics display: total reviews, avg time, rework rate, appeals overturned
5. Profile shows expertise skill tags with verification status
6. Skill verification queue lists pending claims with anonymous contributor identifiers
7. Verify and dispute actions work -- dispute requires reason dialog
8. Messages page is reply-only -- no "New Conversation" or "Compose" button
9. Messages show senderRole ('You'/'Platform') not names
10. Capacity settings: Slider for hours, Switch for temporary pause
11. Skills settings: expertise area checkboxes + skill tag input
12. Notification prefs: 4 categories x 2 channels with Switch toggles
13. Settings root page shows sidebar navigation to 3 sub-routes
14. All forms use native `<label>` elements (not @glimmora/ui Label)
15. GradientCard used for metric cards on profile page (not KPIStatCard)
</verification>

<success_criteria>
- Mentor can see their tier, metrics, and skill tags on the profile page
- Mentor can process skill tag verification requests (verify or dispute with reason)
- Mentor can reply to platform messages but cannot initiate new conversations
- Mentor can adjust their capacity hours and pause their queue
- Mentor can manage expertise areas and notification preferences
- All pages follow established patterns from Women's and University portals
</success_criteria>

<output>
After completion, create `.planning/phases/04-mentor-portal/04-04-SUMMARY.md`
</output>

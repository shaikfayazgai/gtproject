// Contributor notifications reuses the same Notification Centre UI as enterprise.
// The notification store is shared, so users see their own notifications here.
export { default } from "@/app/enterprise/notifications/page";
